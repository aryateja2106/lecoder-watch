<!-- Claude Code reads CLAUDE.md, not AGENTS.md. This file only imports the shared marketing
     brief so every agent working in marketing/ (Claude Code, Codex, Cursor) loads the same
     instructions. Keep it to the one import line: backpass treats that as a pointer. -->
@AGENTS.md
