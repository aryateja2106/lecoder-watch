# Scrollcraft workspace

Builds and the fingerprint registry for the Mesh landing page live here.
Created so `/scrollcraft` does not dump output at the repo root.

`builds/` is gitignored. After the first real build, keep `FINGERPRINTS.md`
so the next landing page cannot recycle this one's grammar.
