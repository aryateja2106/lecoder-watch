# Task board — 0.6 "Agents": chat UI, two build routes, secret redaction (started 2026-09-04)

Working file. Owner: the Fable session in `~/Projects/lecoder-watch-ade` (branch `feat/ade-0.6`).
Deadline: ship by **Sunday 2026-09-06** — TestFlight build + meshd/mesh-install release + landing.
States: `[x]` done · `[~]` running · `[ ]` open · `[!]` blocked on Arya.

## 0. Facts that decide the plan (measured 2026-09-04 morning, IST)

- **Branch truth.** The deployed daemon (`~/.mesh/meshd`, 0.5.4, pid 2155) is byte-identical to
  `origin/claude/mesh-install-pair-46f4f1` (PR #118, 37 commits main lacks: herdr panes, peek
  polish, machine dedupe, smoke gate, `gates.sh`). `origin/main` (0.5.2) has only 3 merge commits
  that branch lacks; `git merge origin/main` into it produced **zero conflicts and an empty diff**.
  So `feat/ade-0.6` = PR #118 branch + main. PR #118 itself targets `backup/2026-07-02` (wrong base).
  `backup/2026-09-03` (the primary checkout) is a snapshot branch: 7 WIP/board commits, meshd 0.5.0 —
  not a base for anything.
- **The other agent's work** (Gemini, uncommitted in the primary checkout) is saved in the session
  scratchpad (`gemini-work/`): `iOS/AgentChatView.swift` (847 lines), a 185-line patch to
  `iOS/TerminalView.swift` + `Watch/WatchViews.swift`, `docs/LOCAL-FIRST-ADE-2026.md`, three skills.
  The Watch hunk applies cleanly to `feat/ade-0.6`; the TerminalView hunk conflicts (PR #118 reworked
  the peek screen). Its chat parser is regex over polled `capture-pane` text and is not wired to
  `/events`; the watch glance re-implements the same regex; `isDangerous` ignores
  `Shared/RiskClassifier.swift`. **Grade (agent, 2026-09-04):** LIVE — Chat/Terminal toggle,
  approval card → `/agents/:n/send`, composer + pills, watch decision glance → `store.send`, watch
  Cursor launcher. DEAD — `<thinking>` and `$ `/`❯ `/`Run: ` tool-card regexes (no agent prints them;
  `❯` is this app's idle-prompt marker); tool status pills never leave `.running`. FAKE — artifact
  cards (`https://localhost/…`, `http://localhost:7777/…`, skills undiscoverable by Claude Code because
  they were never placed in `.claude/skills`). Skills: every Apple API name checked is real; they
  hardcode `B5B87F7AXF`/`com.aryateja`/`:7777`.
- **Data path (agent-mapped, file:line in the session transcript).** meshd is HTTP polling only:
  `GET /agents/:n/output` = capture-pane text snapshot; `POST /agents/:n/send` = keystrokes and is the
  only approval-answer route (Approve = Enter, Decline = Escape). No WebSocket in meshd. The live PTY
  WebSocket is **rmux-bridge** (`install/payload/rmux-bridge`, port 7820), which the iOS Terminal
  mode loads in a WKWebView. The structured channel is `AgentEvent` from `mesh-hook` (Python), which
  is registered only for Claude Code `Notification` + `Stop` and collapses `tool_name`/`tool_input`
  into a title string; `replyable` = the hook ran inside a mux pane. Codex gets a static "turn ended"
  event. cursor-agent, agy, hermes, openclaw: zero wiring.
- **Security (agent-audited).** rmux-bridge `/attach?session=` has **no authentication at all**,
  binds `0.0.0.0`, serves `ws://` — anyone on the LAN/hotspot can read and type into any tmux/rmux
  session. meshd: bearer-only (never in a URL), Host guard, loopback exemption, plain HTTP, binds
  `0.0.0.0`. **No redaction exists anywhere**; the single fan-out for events is `addEvent()` in
  `server.ts` (disk `agent-events.jsonl` → APNs alert → Live Activity `lastLine`). Output lines flow
  unredacted through `/agents/:n/output` → phone → watch (`WCSession` snapshot, which also carries
  every machine token — by design, encrypted pairing channel). Tokens: Keychain on phone/watch,
  0600 files on the Mac, `testtoken` fallback gone. No SSH or VNC credential exists in this codebase
  (VNC is an external noVNC URL).
- **Devcluster reference** (`~/Projects/devcluster-mesh`): Express daemon, zero auth, was bound
  `0.0.0.0:7777` running `cursor-agent -p --yolo --model claude-3-7-sonnet` with cwd `$HOME`.
  **Rebound to 127.0.0.1 this morning** (config.json backed up; pid 83522). OTA (`itms-services://`)
  needs an HTTPS manifest URL → tunnel; we replace OTA with `xcrun devicectl device install app`
  (paired iPhone, Wi-Fi/USB, no HTTPS) and `simctl install` for the simulator proof.
- **CLI agents on this Mac.** `claude` (stream-json in/out, `--permission-prompt-tool`), `codex`
  (`exec --json`, `app-server` JSON-RPC, `mcp-server`), `cursor-agent` (`-p --output-format
  stream-json`, `--resume`), `agy` (stream-json in/out, `--dangerously-skip-permissions`), `hermes`
  (`approvals`, `--yolo`, `acp`, `serve`). `openclaw`, `gemini`: not installed.
- **Ship path.** `scripts/release-testflight.sh` (ASC key `Y4MR7X24UL`, app `6803438426`, bundle
  `com.lecoder.meshwatch`, TestFlight public link `pVYPTxc7`), `scripts/release-mesh-install.sh
  --publish` (mesh-install latest is 0.5.2; the fleet-deployed 0.5.4 was never published). CI:
  daemon `tsc` + 12 checks; apps job builds iOS/watch/macOS + `check-all.sh`. `gates.sh fast` on
  this branch: `FACTORY_GATES: level=fast status=MISCONFIGURED` (stack detection = unknown; gates.sh
  is factory policy, not ours to edit) — the real gate is `sh scripts/check-all.sh` + xcodebuild +
  `scripts/check-ios-smoke.sh`. Xcode 26.6, sim `iOS27-repro` booted.
- **Marketing skills:** none installed beyond higgsfield asset generation; the catalog has
  `shipping-and-launch` (`~/3-Resources/skills-catalog/global/`). Posts get drafted by hand Saturday.

## 1. The release: MeshWatch 0.6 "Agents" + meshd 0.6.0

One sentence: *watch and answer any CLI coding agent from your wrist, and let it build you an app —
native if you have an Apple developer account, a home-screen web app if you don't — with every
secret it prints redacted before it leaves your Mac.*

### Lanes (parallel, one writer per lane; `Shared/**` and `install/payload/meshd/**` are one wire)

| Lane | Writer | Files |
|------|--------|-------|
| D — daemon | Fable (this session) | `install/payload/meshd/**`, `install/payload/rmux-bridge/**`, `install/payload/bin/mesh-hook`, `scripts/check-*.sh` (new only) |
| P — phone + watch | Swift executor (Sonnet), after the deadness grade | `iOS/**`, `Watch/**`, `Shared/**` (coordinated with D on wire shapes) |
| K — skills + CLI | executor (Sonnet) | `.agents/skills/**`, `.cursor/skills/**`, `.claude/skills/**`, `install/payload/bin/mesh` (bash), `docs/**` |
| R — release + landing | Fable + Arya | `CHANGELOG.md`, `project.yml` versions, `web/**`, release scripts (run, not edit) |
| V — verification | `factory-verifier` / fresh Sonnet per slice | read-only; runs the thing |

### S1 — rmux-bridge authentication (P0, security) — lane D + P
- [x] Bridge requires the meshd bearer token on `/`, `/attach` and every asset: `Authorization: Bearer`
  header **or** cookie `mesh_token` (browsers cannot set WS headers), loopback peers exempt exactly like
  meshd's `auth.ts` (`BRIDGE_TRUST_LOOPBACK=0` turns that off). Token from `MESHD_TOKEN` /
  `$MESH_HOME/token`. Fail closed when unset. `/health` answers without auth (`auth: token|loopback-only`).
- [~] iOS `BridgeTerminalScreen` sets the `mesh_token` cookie for the bridge origin before loading (lane P).
- [x] `scripts/check-bridge-auth.sh`: 18 assertions across three throwaway bridges — OK.
- [x] Proved by running: side bridge on :7821 attached to rmux `mesh-test`; cookieless attach closed 1002;
  cookie attach streamed; `echo … ghp_…` came back as `TOKEN_IS ghp_••••••[902dd5] done`.

### S2 — secret redaction + exposure ledger (P0, Arya's explicit ask) — lane D, then P
- [x] `install/payload/meshd/redact.ts`: `redact(text) → {text, findings}`. Rules: PEM private keys,
  AWS `AKIA/ASIA`, GitHub `ghp_/gho_/ghu_/ghs_/ghr_/github_pat_`, Anthropic `sk-ant-`, OpenAI `sk-`,
  Slack `xox?-`, Google `AIza`, Stripe `sk_live/rk_live/sk_test`, HF `hf_`, npm `npm_`, JWT
  `eyJ..eyJ..`, `Authorization: Bearer …`, `scheme://user:pass@`, `key|secret|token|password = …`
  assignments (≥16 chars), plus **exact-match known secrets** (the daemon's own token, every
  `hosts.json` peer token, `*_TOKEN|*_KEY|*_SECRET` env values) — exact match is what catches the
  64-hex meshd token without redacting every sha256 checksum. Replacement keeps the identifying prefix
  and appends a 6-hex fingerprint: `ghp_••••••[a3f9c1]`.
- [x] Ledger `~/.mesh/exposures.json` (0600): fingerprint → `{kind, hint, first, last, count, channels,
  status: open|rotated|ignored}`. Fingerprint = sha256 prefix, never the secret.
- [x] Choke points: `addEvent()` before disk/push/LA; `GET /events` on read (pre-0.6 lines on disk);
  `agentOutput()` per line (10-min dedupe per fingerprint so a key on screen is one exposure, not one per
  poll); rmux-bridge fan-out (partial line held until newline or 40 ms; findings POSTed to meshd
  `/exposures/record` over loopback so one process owns the ledger); chat route text. `agent-events.jsonl`
  written 0600 and chmod'd at boot. `mesh-hook` no longer sends a transcript PATH as the body.
- [x] Routes: `GET /exposures`, `POST /exposures/:fp {status}`, `POST /exposures/record` (bridge);
  `/doctor` gains an informational `exposures` line; capability `"redact"`.
- [x] CLI: `mesh exposures [--rotated|--ignored|--reopen <fp>]` — table of kind/hint/fp/count/last/where; proved against a throwaway daemon (`113e10d`).
- [x] `scripts/check-redact.sh`: 17 rules + wrap artifact + ledger + HTTP surface — OK. Runs in `check-all.sh`.
- [ ] Phone: Settings → **Exposed secrets** list (kind, hint, fingerprint, count, last seen; mark
  rotated). Watch: none.
- [x] Proved by running (side daemon :8898, scratch events file): `POST /events` with a fake `ghp_` and the
  daemon's own token → response, `GET /events` and the on-disk line all read `ghp_••••••[902dd5]` /
  `••••••[2caed2]`; `/exposures` → 2 open (github-token, known-secret MESHD_TOKEN); mark rotated → open 1;
  `/doctor.checks.exposures` reports it; ledger file mode 600. Lock Screen card: needs the phone (lane P / Arya).

### S3 — the chat UI reads real structure (P0) — lane D, then P
- [x] `install/payload/meshd/chat.ts`: `GET /agents/:n/chat?since=&limit=` → `{name, source, cursor, messages}`
  `{id, ts, role: user|assistant|thinking|tool|result|approval|system, text, tool?, status?}`.
  Source per agent, in order: **Claude Code transcript** (`~/.claude/projects/<cwd-slug>/*.jsonl`;
  path from the hook's `transcript_path` when an event carried it, else newest transcript whose
  `cwd` equals the pane's `#{pane_current_path}`), **Codex rollout** (`~/.codex/sessions/**`, reuse
  `codex-state.ts` readers), fallback **output lines** as `role: system` terminal blocks (cursor-agent,
  agy, hermes until their transcript readers land). Capability `"chat"`. **cursor-agent transcripts
  landed too** (`~/.cursor/projects/<slug>/agent-transcripts/<chat>/<chat>.jsonl`, Claude-shaped blocks).
  Proved by running on the side daemon: `/agents/mesh-test/chat?limit=3` → `source: claude`, real
  tool/result messages with `status: ok`; `since=<cursor>` → empty page. Parsers checked on a 155-message
  Claude transcript and a 52-message cursor transcript from this Mac; Codex parser written against a
  real rollout's shapes (no rollout for the test cwd existed to run it — verify when one does).
- [x] `mesh-hook`: forwards `transcriptPath`, `toolName`, `hookEvent` (additive; `check-mesh-hooks.sh` OK);
  meshd remembers the transcript per session and the chat route reads it first.
- [ ] Approval card = latest event for the session is `needs-input` and `replyable`, or `/agents` row
  `status == waiting`; Allow → existing `respondToAgent` (Enter), Deny → Escape. **Delete the `[y/n]`
  regex** in both `AgentChatView` and the watch glance; both read the same status field.
- [ ] Watch `AgentLiveView` decision glance reads `status`, haptic Allow/Deny → `WatchMeshStore.send`.
- [ ] Phone `AgentChatView`: poll `/agents/:n/chat` at the peek cadence; keep terminal-block fallback;
  wire `isDangerous` to `RiskClassifier.classifyRisk`; composer → `/agents/:n/send` (paste=true for
  multiline). Chat/Terminal toggle stays.
- [ ] Prove by running: `claude` in a meshd session on this Mac, ask it to read a file; phone sim shows
  user → thinking → tool card → assistant; approve a Bash call from the sim; watch sim shows the glance.

### S4 — launch any CLI agent from the phone/watch (P1, small) — lane D + P
- [x] `/doctor` reports which agent CLIs exist on the Mac: top-level `agents: [{name, path}]` plus an
  informational `checks.agents` line (`Bun.which` over claude, codex, cursor-agent, agy, hermes, openclaw;
  this Mac → 5 of 6). `/agents/new` already takes `cmd`.
- [~] Launcher pills show only installed ones (lane P, gate on `/doctor.agents`).

### S5 — the skills your agents need (P0) — lane K
- [x] Rewrite `native-app-builder`: no hardcoded Team ID / `com.aryateja` / `localhost:7777`. Team +
  prefix come from `mesh apps config` (stored `~/.mesh/apps.json`) or the prompt; XcodeGen +
  `xcodebuild -destination 'generic/platform=iOS' -allowProvisioningUpdates
  -allowProvisioningDeviceRegistration`; install with `mesh apps install <slug>` (devicectl to the paired
  iPhone, simctl to a booted sim); TestFlight via `asc` only when the user asks; emit
  `<!-- APP_READY slug= name= -->`.
- [x] Rewrite `pwa-local-app-builder`: single-folder static app, IndexedDB/OPFS, iOS meta tags, manifest,
  service worker **only** works over HTTPS/localhost — say so; publish with `mesh apps publish <dir>
  --slug --name` (prints the LAN URL); emit `<!-- PWA_READY slug= name= url= -->`. Add-to-Home-Screen
  instructions; offline requires HTTPS hosting (documented option: user's own Cloudflare/Vercel/GitHub
  Pages).
- [x] `apple-native-apis`: API names spot-verified against Apple docs; `:7777` fake routes replaced with real
  meshd routes; "how MeshWatch itself uses these" section added. Canonical home is
  `install/payload/share/skills/`; `.claude/skills` and `.cursor/skills` are committed symlinks;
  `.agents/skills` symlinks exist locally but `.gitignore` excludes them by earlier policy (Arya: keep?).
  `docs/LOCAL-FIRST-ADE-2026.md` folded into `docs/local-first-ade.md` (corrected) + index row.
- [x] `mesh skills install|status [--uninstall]`: copies into `~/.agents/skills/`, symlinks the three
  client dirs, never overwrites a user-owned directory; `install.sh` calls it after the hooks (proved with
  `HOME=<tmp> sh install/install.sh --only tools --no-start` → 3/3 installed, 3/3 linked).
  `scripts/check-mesh-skills.sh` OK.
- [x] `mesh apps config|publish|add|list|install|remove` (bash); mutating commands end on `ok …`/`error …`;
  device pick requires `reality: physical` AND `tunnelState: connected` (a paired-but-away iPhone was
  otherwise chosen over a booted sim — found and fixed live). `scripts/check-mesh-apps.sh` OK. Unverified:
  a real signed `.app` through `simctl`/`devicectl` (needs a built app — S6 proof step).

### S6 — apps hosting + install (P0 PWA, P1 native) — lane D + P
- [x] meshd (`apps.ts`): reads `~/.mesh/apps/<slug>/meta.json`; `GET /built-apps` (auth) lists without the
  key or the Mac `.app` path; PWA served **without bearer** at `/a/<slug>-<8-hex key>/…` (mounted before
  the auth gate, after the Host guard; wrong key / unknown slug / `..` / encoded `..` / symlink out of
  `site/` all 404 identically; html no-cache, assets 5 min); `POST /built-apps/:slug/install {target}` shells
  out to `mesh apps install <slug> [--sim]` (one implementation, lane K's CLI). Capability `"apps"`.
  `scripts/check-apps-serve.sh` — 30 assertions on a throwaway daemon — OK.
- [ ] Chat artifact card (exists in `AgentChatView`) → **Open** (Safari, so Add to Home Screen works) /
  **Install to iPhone** → these routes. Home tab "Apps" list from `GET /built-apps` (renamed: `/apps` is the Mac app switcher — lane P caught the collision).
- [ ] Prove by running: publish a 3-file PWA, open on the phone sim's Safari; build the
  `prototypes/mlx-coder` sample (or a 1-view app) and `simctl install` it.

### S7 — release (P0) — lane R
- [ ] Versions 0.6.0 (`project.yml` MARKETING_VERSION, meshd `VERSION`, capability strings), CHANGELOG
  `[Unreleased]` → 0.6.0 written for humans.
- [x] `sh scripts/check-all.sh` green on this Mac (53 OK; the one FAIL — chat composer without
  `.shellSafe` — fixed in `34c319a`); iOS, watch and MeshDesktop builds green; `MESH_SMOKE_REQUIRED=1
  sh scripts/check-ios-smoke.sh` → `check-ios-smoke: OK — the app launches, every tab renders, and a
  text field can appear` (3 UI tests on iOS27-repro). CI on PR #123: **green** (both jobs, two runs).
- [x] Draft PR from `feat/ade-0.6` → `main`: **#123** (https://github.com/aryateja2106/lecoder-watch/pull/123); #118 told. CI pending at first push.
- [!] Arya: `ASC_KEY_ID=Y4MR7X24UL ASC_ISSUER_ID=… sh scripts/release-testflight.sh` (Keychain prompt),
  then Beta App Review for the public link; `sh scripts/release-mesh-install.sh --publish`; merge.
- [ ] Landing `web/`: the one-sentence story above, install one-liner, TestFlight link, a 20-second
  recording of chat → approve → app appears.

### S8 — go-to-market (P1, Saturday) — lane R
- [x] Launch kit drafted: `docs/launch-0.6.md` — TestFlight "What to Test", 8-post X thread, LinkedIn, Show HN,
  20-second demo script, and a "do not say" list (offline, any-agent, encrypted). Assets via higgsfield
  skills still to make; pricing stays out of this release (decision deferred by Arya).

## 2. Explicitly not in 0.6
TLS for meshd/bridge (LAN + bearer today; ticket/TLS later) · OTA `itms-services` (devicectl replaces it)
· transcript readers for cursor-agent/agy/hermes (output fallback) · structured Live Activity content ·
voice (#110) · local brain (#119 lands on its own; keep `/brain` untouched) · pricing.

## 3. Log
- 2026-09-04 18:1x IST — **first device test (Arya, iPhone 15 Pro, live daemon upgraded to 0.6.0).** Verdict
  "25% of what we wanted". Bugs found and fixed: Chat typed LF instead of Enter (nothing submitted →
  no transcript → screen fallback) `83a43fe`; bridge probe treated 401 as "no bridge" `6aad040`; the
  terminal page's Esc/arrow buttons sent the literal text `\u001b[A` (0.5-era) and the page never
  reconnected `dd29b7f` (deployed live, no app rebuild); Chat gained the key row. Agent behaviour: asked
  for a habit tracker, got an unrequested web app + a parallel native build + no icon + tests looping in
  background jobs → new `app-brief` interview-first skill, definition of done, icon generator `ae7d716`.
  Docs pass: `docs/compare-moshi.md` (sourced; the other agent's draft invented Moshi's price),
  ROADMAP "0.6.x from the device test", README 0.6 truth, AGENTS.md routes + two rules, CHANGELOG.
  Lane P2 (Sonnet) rebuilding Chat: collapsed tool cards, markdown, jump-to-latest, keyboard dismiss,
  no Y button, two-step Stop, approval card that hides after a tap, Safari long-press text, WebView
  reload on foreground. Voice draft (other agent) being graded for porting. Still open from the
  feedback: gestures/chords, model picker, Mac permission onboarding, friendly URLs, background-job
  visibility, "watch screen button" (unidentified — need a screenshot).
- 2026-09-04 13:0x IST — **CI green on PR #123** (daemon 20 s; apps/Xcode 11m41s and 15m41s, both runs).
  First run was red on one new check: `check-mesh-skills.sh` demanded `.agents/skills/<name>` symlinks
  that `.gitignore` keeps out of every checkout; the check now asserts only the committed `.claude` and
  `.cursor` pointers (`630916c`). Smoke gate green locally. Open for Arya: TestFlight upload, Beta App
  Review, `release-mesh-install.sh --publish`, merge; decide whether `.agents/skills` should be tracked;
  device proofs (Live Activity card, devicectl install of a built app, watch Allow on a real prompt).
- 2026-09-04 12:3x IST — second fresh-context verifier (skills/CLI + Swift): **PASS, high confidence, 0
  blockers** — every CLI command re-run in its own temp HOME (publish URL, meta.json, slug rejection,
  `--sim` reaches real `simctl` and fails clean on a fake .app, install idempotent, `--uninstall` spares
  user-owned dirs), every Swift wiring claim grepped (status-driven approvals, `classifyRisk`, `key:
  "ctrl-c"`, `.shellSafe`, cookie before load, `/built-apps` vs `/apps`), both sim builds green, app
  launched on the sim. Notes: Settings → Exposed secrets is inside `if !store.machines.isEmpty` (by design:
  per-machine data); the shared sim had lane P's pairing to the REAL fleet cached — app uninstalled from
  `iOS27-repro` afterwards. Draft PR #123 open; `mesh exposures` CLI and the launch kit pushed; smoke
  gate running.
- 2026-09-04 12:0x IST — fresh-context verifier on lane D: **PASS, high confidence**, five throwaway
  processes, every claim re-run. Two defects, both fixed in the follow-up commit: D1 the url-credentials
  rule re-matched its own mask when stored events were re-redacted on read (fingerprint drift; `•` now
  excluded from every open-ended class, idempotency fixtures added); D2 the soft-wrap unsplit lived only in
  the bridge (moved into `redact()` itself so events, chat and output get it). Lane K done (6 commits),
  lane P done (5 commits; caught the `/apps` collision with the Mac app switcher → hosted apps moved to
  `/built-apps`). iOS, watch and MeshDesktop builds green. History rewritten to drop the scanner-flagged
  fixture; pushing. Second verifier running on lanes K + P.
- 2026-09-04 10:5x IST — lane D also landed S6 daemon (`apps.ts` + check), S4 doctor agents, meshd 0.6.0,
  landing + privacy copy, CHANGELOG 0.6 entries. Lane P has 4 commits (chat view, bridge cookie, exposed
  secrets screen, apps list). **Push to origin blocked by GitHub push protection** — a Stripe-shaped test
  fixture in commit `a6a2f81`; fixed forward in `a08f1f3`, history rewrite queued for when every lane is
  idle (`scratchpad/rewrite-stripe-fixture.sh`), then push + draft PR (`scratchpad/pr-body.md`).
  `asc auth status`: key Y4MR7X24UL in the System Keychain — `sh scripts/release-testflight.sh` needs no
  env vars. Fresh-context verifier running on the daemon commits.
- 2026-09-04 10:0x IST — lane D: S1 (bridge auth), S2 (redaction + ledger), S3-daemon (chat route) built,
  self-checked and proved live on a side daemon (:8898) and side bridge (:7821) against rmux `mesh-test`.
  Lanes K (skills + CLI) and P (Swift) running in parallel in this worktree.
- 2026-09-04 09:2x IST — devcluster daemon rebound to loopback. `feat/ade-0.6` created from PR #118's
  branch + main (empty merge). Gemini's work snapshotted to scratchpad. Five explore agents: data path,
  deadness, devcluster, secrets, ship path — four done, deadness pending.
