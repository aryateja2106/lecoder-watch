## Why

A spoken knowledge base is a note the person can ask for later, not a second copy of the mesh. The owner's boundary for this slice is the same as the account slice: the PDF and the note stay on the machine, and a held app draft waits for confirm before any further command. The person this is for does not configure SSH keys, port forwarding, a VPN, or a speech cloud. An executing agent does not stand in for a physical microphone or speaker.

## What Changes

- Add a contract other agents can execute for the spoken knowledge base and the held app draft, without a second implementation of work that already exists on draft pull requests.
- Name the in-place note replace and the replaced-note draft as finished. `POST /knowledge/:id` replaces that note's body in the same file. After that replace, the agent-note path drafts the new body. Do not rebuild either slice.
- Name the replaced-note hold and the spoken note replace as finished. A replaced note that asks to send a pairing code or copy hosts.json stays on hold and the model is not called. "summarize this paper" still drafts the new body. `POST /knowledge/:id` accepts `{ audio }`, and a local `MESH_STT` transcript replaces that note body. Do not rebuild either slice. Do not add a second `/knowledge` route.
- Name the spoken-note draft as finished. Pull request 187 adds only `scripts/check-spoken-note-draft.sh`. A local `MESH_STT` transcript replaces one note and the agent-note path drafts that new body to `held-note.txt`, mode 600, unexecuted. A pairing-code transcript and a hosts.json transcript stay on hold and the loopback model is not called. "summarize this paper" drafts the new body. A missing id does not call the model. A remote audio path does not write. The check passed on `127.0.0.1:8898` with the gateway key unset. Daemon CI and Xcode CI are both green on `0baf377`. This does not prove a microphone or a speaker. Do not rebuild that check. Do not add a second `/knowledge` route.
- Name the spoken replace speak as finished. Pull request 189 speaks a replaced note with a local TTS binary. `POST /knowledge/:id` accepts `{ audio, speak }`. When `speak` is true, the title plus the new transcript is handed to a local `MESH_TTS` binary. `spoken` is true only when that binary is a local file and the process exits 0. `speak: false`, or a missing TTS binary, stores the transcript and leaves `spoken` false. A remote URL, a scheme, or a protocol-relative path for the audio or for `MESH_TTS` returns before `MESH_STT` and before any write. An empty transcript keeps the old body and does not speak. A missing id creates nothing. The title stays. The list stays `{ id, title }`. `sh scripts/check-spoken-replace-speak.sh` passed on `127.0.0.1:8898`. Daemon CI and Xcode CI are both green on `611f321` (check run `35714316679`). This does not prove a microphone or a speaker. Do not rebuild that speak path. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway. Pull request 192 (tip `6faf3f3`) already named pull request 189.
- Name the spoken replace draft as finished. Pull request 193 adds only `scripts/check-spoken-replace-draft.sh`. `speak: true` with local `MESH_STT` and local `MESH_TTS` returns `spoken` true and drafts that new transcript to a held file, mode 600, unexecuted. A pairing-code transcript and a hosts.json transcript stay on hold. "summarize this paper" still drafts. A missing id and a remote audio or TTS path do not write and do not call the model. There is no `liveGatewayCall`. This does not prove a microphone or a speaker. Do not rebuild that check. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway. Pull request 194 (tip `cfe2b17`) already named pull request 193.
- Name the local note search as finished. Pull request 195 matches `GET /knowledge?q=` against the title or the body, case-insensitive, and returns only `{ id, title }`. A missing or empty `q` returns every note as `{ id, title }`. A remote URL, a scheme, a protocol-relative value, or any string containing `://` is 400 and is not opened. A pairing-code match and a hosts.json match do not return those bodies. `POST /agent-note` still drafts only the matching paper note to one relative file, mode 600, not executed. A command without confirm does not run. `server.ts` was not edited. `handleKnowledge` is still one call. `sh scripts/check-local-note-search.sh` passed on `127.0.0.1:8898` at `1238daa` with the gateway key unset. Daemon CI and Xcode CI are both green on `1238daa` (check run `35736830546`). This does not prove a microphone or a speaker. Do not rebuild that search. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the query-note draft as finished. Pull request 197 accepts a local `q` on `POST /agent-note` when `id` is absent and selects with `listNotes`. One match drafts one relative file, mode 600, not executed. Zero matches is 404. Several matches is 409. A remote or empty `q` is 400. None of those call the model. The hold matches `textMovesSecret` (pairing code, hosts.json, mesh token, `.mesh/token`, and the upload/send sentence) and does not import `route.ts` or `filter.ts`. `id` without `q` still drafts. `server.ts` was not edited. `bun x tsc --noEmit -p install/payload/meshd/tsconfig.json` and `sh scripts/check-query-note-draft.sh` on `127.0.0.1:8898` at `e11483d` both exited 0. The gateway key was unset. Daemon CI and Xcode CI are both green on `e11483d` (check run `35739420177`). Daemon CI and Xcode CI are both green on handoff tip `1e78b081f2c64b038008054bedad1fd3a037ec6b` (check run `35740060386`). Daemon CI and Xcode CI are both green on handoff tip `c140ce7bf73c7f979a28d55d9a4784cc723e0027` (check run `35741550431`). Daemon CI and Xcode CI are both green on handoff tip `978bdbe17099174c5ff5f4cb5b401fa7f6555f6c` (check run `35743157250`). Daemon CI and Xcode CI are both green on handoff tip `dcfa77f5894058d1b815a0400c7396b17e356f87` (check run `35744449505`). Daemon CI and Xcode CI are both green on handoff tip `b3e91609e5a8c488dded8cc4d3aeae1e840c1b2e` (check run `35745531307`). Daemon CI and Xcode CI are both green on handoff tip `2aad3fd833efbf725f57c9e3c76fbf470a07d44a` (check run `35746528962`). Daemon CI and Xcode CI are both green on handoff tip `bd904b19dc5dc85752888193ad365c2d3a176c8a` (check run `35747820842`). This does not prove a microphone or a speaker. Do not rebuild that draft. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the local PDF note as finished. Pull request 198 accepts `{ pdf, title? }` on `POST /knowledge`. `MESH_PDF` must be a local executable. Its stdout is the note body. A remote value, a value containing `://`, a missing file, a non-executable, empty stdout, or a nonzero exit writes nothing. The list stays `{ id, title }`. `server.ts` was not edited. `sh scripts/check-local-pdf-note.sh` and `sh scripts/check-local-note-search.sh` on `127.0.0.1:8898` at `42211d0` both exited 0. The proof is a stub executable. A `path` that is a real PDF still uses the in-daemon `extractPdf` scrape. Do not rebuild either path. Daemon CI and Xcode CI are both green on `42211d0` (check run `35742140469`). This does not prove a microphone, a speaker, or that a paper was read. Do not call the Vercel AI Gateway.
- Name the PDF note draft as finished. Pull request 199 adds only `scripts/check-pdf-note-draft.sh`. A stub `MESH_PDF` sentence that includes "summarize this paper" becomes one note. A query with no id drafts it to one relative file, mode 600, not executed. A pairing-code note and a hosts.json note stay on the existing hold and the model is not called. A pdf value containing `://` and a remote `MESH_PDF` write nothing. `server.ts` was not edited. `sh scripts/check-pdf-note-draft.sh` on `127.0.0.1:8898` at `bd04cc0` exited 0. Daemon CI and Xcode CI are both green on `bd04cc0` (check run `35743446950`). This does not prove a microphone, a speaker, or that a paper was read. Do not rebuild that check. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the PDF note speak as finished. Pull request 200 accepts `{ pdf, title?, speak? }` on `POST /knowledge`. When `speak` is true, a local `MESH_TTS` that exits 0 returns `spoken: true`. A remote value, a value containing `://`, or a protocol-relative `MESH_TTS` returns 400 before `MESH_PDF` runs and writes nothing. When `speak` is omitted, the response stays `{ id, title }` and TTS does not start. A missing or failing TTS binary still stores the note and returns `spoken: false`. `server.ts` was not edited. The files are `install/payload/meshd/knowledge.ts` and `scripts/check-pdf-note-speak.sh` only. `sh scripts/check-pdf-note-speak.sh`, `sh scripts/check-local-pdf-note.sh`, and `sh scripts/check-pdf-note-draft.sh` on `127.0.0.1:8898` at `d4486c4` all exited 0. Daemon CI and Xcode CI are both green on `d4486c4` (check run `35746494919`). This does not prove a speaker played audio. Do not rebuild `extractPdf` or `MESH_PDF`. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the note ask draft as finished. Pull request 201 accepts an optional local `ask` on `POST /agent-note`. The model receives the note, a blank line, and the ask. An ask that would move a pairing code, `hosts.json`, `.mesh/token`, or a mesh token is held before the model. An ask containing `://` writes nothing. A missing ask still drafts the note text only. `knowledge.ts` was not edited. `server.ts` was not edited. The files are `install/payload/meshd/agent-note.ts` and `scripts/check-note-ask.sh`. `sh scripts/check-note-ask.sh`, `sh scripts/check-query-note-draft.sh`, and `sh scripts/check-pdf-note-draft.sh` on `127.0.0.1:8898` at `ae83b95` all exited 0. Daemon CI and Xcode CI are both green on `ae83b95` (check run `35747085140`). Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the spoken note ask as finished. Pull request 202 adds only `scripts/check-spoken-note-ask.sh` in the new commit, after merging pull request 201. A stub `MESH_PDF` sentence is spoken by a local `MESH_TTS` that exits 0, then ask "build a reader" drafts one relative file, mode 600, not executed. A remote `MESH_TTS` returns 400 and does not start `MESH_PDF`. A pairing-code ask does not call the model. A command without confirm does not run. `server.ts` was not edited. `sh scripts/check-spoken-note-ask.sh`, `sh scripts/check-pdf-note-speak.sh`, and `sh scripts/check-note-ask.sh` on `127.0.0.1:8898` at `b4ce6ac` all exited 0. Daemon CI and Xcode CI are both green on `b4ce6ac` (check run `35748224297`). This does not prove a speaker played audio or that a paper was read. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the note reply save as finished. Pull request 203 saves an allowed local ask as one knowledge note. The title is the ask, clipped to 200 characters, and the held file is still written. A missing or whitespace ask adds no note. A pairing-code ask, or a reply the route would hold, writes neither the file nor a draft. `server.ts` was not edited. The files are `install/payload/meshd/agent-note.ts`, `install/payload/meshd/knowledge.ts`, `scripts/check-note-reply-save.sh`, and `scripts/check-spoken-note-ask.sh`. `sh scripts/check-note-reply-save.sh`, `sh scripts/check-spoken-note-ask.sh`, `sh scripts/check-note-ask.sh`, and `sh scripts/check-pdf-note-speak.sh` on `127.0.0.1:8898` at `92d776c` all exited 0. Daemon CI and Xcode CI are both green on `92d776c` (check run `35749935885`). Daemon CI and Xcode CI are both green on handoff tip `8fce18d` (check run `35749871537`). Daemon CI and Xcode CI are both green on handoff tip `3ad2a7d` (check run `35752461256`). Daemon CI and Xcode CI are both green on `b4ce6ac` (check run `35748224297`). Daemon CI and Xcode CI are both green on `ae83b95` (check run `35747085140`). This does not prove a speaker or that a paper was read. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the reply note draft as finished. Pull request 204 adds only `scripts/check-reply-note-draft.sh`. A later local query that matches only the saved reply drafts that reply to one relative file, mode 600, not executed. A query that matches the original note and the reply returns 409 and writes nothing. A pairing-code ask saves no note, and a query for that ask does not draft. `server.ts` was not edited. `sh scripts/check-reply-note-draft.sh`, `sh scripts/check-note-reply-save.sh`, `sh scripts/check-spoken-note-ask.sh`, and `sh scripts/check-note-ask.sh` on `127.0.0.1:8898` at `4d6ddbb` all exited 0. Daemon CI and Xcode CI are both green on `4d6ddbb` (check run `35750995976`). Do not claim CI on `fb0aab8`. Daemon CI is green on handoff tip `97278aa` (check run `35751643074`). Apps (Xcode) was cancelled on that run. Do not claim both jobs are green on `97278aa`. Daemon CI and Xcode CI are both green on handoff tip `8fce18d` (check run `35749871537`). Daemon CI and Xcode CI are both green on handoff tip `3ad2a7d` (check run `35752461256`). Daemon CI and Xcode CI are both green on `b4ce6ac` (check run `35748224297`). Daemon CI and Xcode CI are both green on `ae83b95` (check run `35747085140`). Daemon CI and Xcode CI are both green on `92d776c` (check run `35749935885`). This does not prove a speaker or that a paper was read. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the saved reply ask as finished. Pull request 205 adds only `scripts/check-saved-reply-ask.sh`. An allowed ask saves one reply note. A later `POST /agent-note` with no id, a `q` that matches only that reply, and a new local ask sends the saved reply, a blank line, and the ask. It writes one relative file, mode 600, not executed, and saves the new reply as another note. A pairing-code second ask writes nothing. A second ask containing `://` is 400 and writes nothing. A `q` that matches the original note and the saved reply is 409 and writes nothing. The list stays `{ id, title }`. `server.ts` was not edited. `sh scripts/check-saved-reply-ask.sh`, `sh scripts/check-reply-note-draft.sh`, and `sh scripts/check-note-ask.sh` on `127.0.0.1:8898` at `294d252` all exited 0. The gateway key was unset. Daemon CI and Xcode CI are both green on `294d252` (check run `35753000381`). Daemon CI and Xcode CI are both green on `4d6ddbb` (check run `35750995976`). Daemon CI and Xcode CI are both green on handoff tip `3ad2a7d` (check run `35752461256`). Daemon CI and Xcode CI are both green on handoff tip `8fce18d` (check run `35749871537`). Daemon CI and Xcode CI are both green on `b4ce6ac` (check run `35748224297`). Daemon CI and Xcode CI are both green on `ae83b95` (check run `35747085140`). Daemon CI and Xcode CI are both green on `92d776c` (check run `35749935885`). Daemon CI is green on handoff tip `97278aa` (check run `35751643074`). Apps (Xcode) was cancelled on that run. Do not claim both jobs are green on `97278aa`. This does not prove a speaker or that a paper was read. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the second reply draft as finished. Pull request 206 adds only `scripts/check-second-reply-draft.sh`. `agent-note.ts` was not edited. An allowed ask saves reply A. A later `POST /agent-note` with no id, a `q` that matches only A, and a new allowed ask sends A, a blank line, and the ask. It writes one relative file, mode 600, not executed, and saves reply B. A later `q` that matches only B drafts B to one relative file, mode 600, not executed. That draft is not A and not the original note. A `q` that matches A and B is 409 and writes nothing. A pairing-code ask on the second hop writes nothing. The list stays `{ id, title }`. `server.ts` was not edited. `sh scripts/check-second-reply-draft.sh`, `sh scripts/check-saved-reply-ask.sh`, and `sh scripts/check-reply-note-draft.sh` on `127.0.0.1:8898` at `0de3ba9` all exited 0. The gateway key was unset. Daemon CI and Xcode CI are both green on `0de3ba9` (check run `35754767900`). Daemon CI and Xcode CI are both green on handoff tip `168d08e` (check run `35754719132`). Daemon CI and Xcode CI are both green on handoff tip `1aca048` (check run `35755761670`). Daemon CI and Xcode CI are both green on `294d252` (check run `35753000381`). Daemon CI and Xcode CI are both green on `4d6ddbb` (check run `35750995976`). Daemon CI and Xcode CI are both green on handoff tip `3ad2a7d` (check run `35752461256`). Daemon CI is green on handoff tip `97278aa` (check run `35751643074`). Apps (Xcode) was cancelled on that run. Do not claim both jobs are green on `97278aa`. This does not prove a speaker or that a paper was read. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the model class draft as finished. Pull request 207 adds only `scripts/check-model-class.sh`. `agent-note.ts` was not edited. An allowed ask with a loopback model returns `modelClass` `local`, writes one relative file, mode 600, not executed, and the JSON has no `sk-secret`. A pairing-code ask with `https://llm.example/v1` returns `modelClass` `user-subscription`, `held` true, `draft` null, and the stub hit count does not increase. An allowed ask with `http://user:sk-secret@127.0.0.1:<stub>/v1` returns `modelClass` `local`. The stub URL has no userinfo. The stub body and the JSON do not contain `sk-secret`. The knowledge list stays `{ id, title }`. `server.ts` was not edited. `sh scripts/check-model-class.sh` and `sh scripts/check-second-reply-draft.sh` on `127.0.0.1:8898` at `48f1970` both exited 0. The gateway key was unset. Apps (Xcode) failed on `48f1970` (check run `35757430003`). Self-checks failed because `scripts/check-model-class.sh` read `/proc`, which macOS does not have, and port 8898 stayed in use. Daemon CI stayed green. Do not claim both jobs are green on `48f1970`. The fix is not on that tip. Daemon CI and Xcode CI are both green on `0de3ba9` (check run `35754767900`). This does not prove a speaker or that a paper was read. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the note that omits the model URL as finished. Pull request 208 adds only `scripts/check-note-omits-model-url.sh`. `agent-note.ts` and `knowledge.ts` were not edited. An allowed ask with `http://user:sk-secret@127.0.0.1:<stub>/v1` returns `modelClass` `local`, writes one relative file, mode 600, not executed, and saves one knowledge note whose title is the ask and whose body is the model reply only. No file under `MESHD_STATE` and no held draft contains `sk-secret`, `user:sk-secret`, or that raw model URL. A pairing-code ask with `https://llm.example/v1` returns `modelClass` `user-subscription`, `held` true, `draft` null, does not call the stub, and no file under `MESHD_STATE` contains `llm.example` or `sk-secret`. The knowledge list stays `{ id, title }`. `server.ts` was not edited. `sh scripts/check-note-omits-model-url.sh` and `sh scripts/check-model-class.sh` on `127.0.0.1:8898` at `890a704` both exited 0. The gateway key was unset. Daemon CI is green on `890a704` (check run `35759364727`). Apps (Xcode) is still running on that run. Do not claim both jobs are green on `890a704`. Pull request 208 tip is now `0a1b0e2cd13348983ec20954a9590259df58347c`. It merges `5f6d6b0` into the note-URL branch. No conflicts. History was not rewritten. `sh scripts/check-note-omits-model-url.sh` and `sh scripts/check-model-class.sh` exited 0 on `127.0.0.1:8898`. After each, `lsof -nP -iTCP:8898 -sTCP:LISTEN` printed nothing. Daemon CI and apps (Xcode) are both green on `0a1b0e2` (check run `35759941279`). Pull request 209 tip is now `bbff8dae533cf0373523d28935e758b3101419b7`. It merges `0a1b0e2` into the subscription branch. No conflicts. History was not rewritten. `sh scripts/check-subscription-note.sh` and `sh scripts/check-model-class.sh` exited 0 on `127.0.0.1:8898`. After each, nothing was listening on port 8898. Daemon CI and apps (Xcode) are both green on `bbff8da` (check run `35760223545`). Pull request 210 is https://github.com/aryateja2106/lecoder-watch/pull/210 on `cursor/subscription-reply-draft-e469`, base `cursor/subscription-note-draft-e469` (still `bbff8dae533cf0373523d28935e758b3101419b7`), tip `10da6b70c980660da264e19126b5272eef0c3de3`; the only added file is `scripts/check-subscription-reply.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-reply.sh` and `sh scripts/check-subscription-note.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; daemon CI and apps (Xcode) are both green on `10da6b7` (check run `35761104930`); this does not prove a speaker or that a paper was read. Pull request 211 is https://github.com/aryateja2106/lecoder-watch/pull/211 on `cursor/subscription-reply-ask-e469`, base `cursor/subscription-reply-draft-e469` (still `10da6b70c980660da264e19126b5272eef0c3de3`), tip `785bb6f9c33df2f28d79339360b2a073599317fa`; the only added file is `scripts/check-subscription-reply-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-reply-ask.sh` and `sh scripts/check-subscription-reply.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; daemon CI and apps (Xcode) are both green on `785bb6f` (check run `35762574523`); this does not prove a speaker or that a paper was read. Pull request 212 is https://github.com/aryateja2106/lecoder-watch/pull/212 on `cursor/subscription-second-reply-e469`, base `cursor/subscription-reply-ask-e469` (still `785bb6f9c33df2f28d79339360b2a073599317fa`), tip `29d68882b05952f238206e68ff9fd1c5a858d3c7`; the only added file is `scripts/check-subscription-second-reply.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-second-reply.sh` and `sh scripts/check-subscription-reply-ask.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; daemon CI and apps (Xcode) are both green on `29d6888` (check run `35763737894`); this does not prove a speaker or that a paper was read. Pull request 213 is https://github.com/aryateja2106/lecoder-watch/pull/213 on `cursor/subscription-reply-confirm-e469`, base `cursor/subscription-second-reply-e469` (still `29d68882b05952f238206e68ff9fd1c5a858d3c7`), tip `e424ae27f6aa08a41ae09d0f32e3b69d788ee82e`; the only added file is `scripts/check-subscription-reply-confirm.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-reply-confirm.sh` and `sh scripts/check-subscription-second-reply.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; daemon CI and apps (Xcode) are both green on `e424ae27` (check run `35765862595`); this does not prove a speaker or that a paper was read. Pull request 214 is https://github.com/aryateja2106/lecoder-watch/pull/214 on `cursor/subscription-confirm-draft-e469`, base `cursor/subscription-reply-confirm-e469` (still `e424ae27f6aa08a41ae09d0f32e3b69d788ee82e`), tip `a6bbd0205682d33c40a5b5900ba8567bbe8f9e92` (parent `7dbdbcb942fac63183aba3cc4bb764e7bf325ef0`); the only added file versus the confirm tip is `scripts/check-subscription-confirm-draft.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-confirm-draft.sh` and `sh scripts/check-subscription-reply-confirm.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; the later query is `margin-token-only` and matches only that confirmed ask; daemon CI and apps (Xcode) are both green on `a6bbd02` (check run `35768647989`); this does not prove a speaker or that a paper was read. Pull request 215 is https://github.com/aryateja2106/lecoder-watch/pull/215 on `cursor/subscription-confirm-ask-e469`, base `cursor/subscription-confirm-draft-e469` (still `a6bbd0205682d33c40a5b5900ba8567bbe8f9e92`), tip `5f2e35fabf971041c385126251908c4a98f90794`; the only added file is `scripts/check-subscription-confirm-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-confirm-ask.sh` and `sh scripts/check-subscription-confirm-draft.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a later ask `margin-ask-only` of only the confirmed note does not run the command, and `margin-ask-true` runs it only when confirm is true; daemon CI and apps (Xcode) are both green on `5f2e35f` (check run `35770492947`); this does not prove a speaker or that a paper was read. Pull request 216 is https://github.com/aryateja2106/lecoder-watch/pull/216 on `cursor/subscription-ask-draft-e469`, base `cursor/subscription-confirm-ask-e469` (still `5f2e35fabf971041c385126251908c4a98f90794`), tip `b584480daf55c67070aa96a202b4f8138f2018d5`; the only added file is `scripts/check-subscription-ask-draft.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-ask-draft.sh` and `sh scripts/check-subscription-confirm-ask.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a later query of only the note titled `margin-ask-only` does not run the command, and confirm true creates `held-saved-ran`; daemon CI and apps (Xcode) are both green on `b584480` (check run `35771113323`); this does not prove a speaker or that a paper was read. Pull request 217 is https://github.com/aryateja2106/lecoder-watch/pull/217 on `cursor/subscription-saved-ask-e469`, base `cursor/subscription-ask-draft-e469` (still `b584480daf55c67070aa96a202b4f8138f2018d5`), tip `fa36116ad9aad19b40e50e1afe503e517de980c1`; the only added file is `scripts/check-subscription-saved-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-saved-ask.sh` and `sh scripts/check-subscription-ask-draft.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a further ask `margin-saved-ask` of only the saved note does not run the command, and `margin-saved-true` runs it only when confirm is true; daemon CI and apps (Xcode) are both green on `fa36116` (check run `35771883445`); this does not prove a speaker or that a paper was read. Pull request 218 is https://github.com/aryateja2106/lecoder-watch/pull/218 on `cursor/subscription-secret-reply-e469`, base `cursor/subscription-saved-ask-e469` (still `fa36116ad9aad19b40e50e1afe503e517de980c1`), tip `e07856f2750bcdec4dde54bf3ab772361d6bd998`; the only added file is `scripts/check-subscription-secret-reply.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-secret-reply.sh` and `sh scripts/check-subscription-saved-ask.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a user-subscription reply that names a pairing code stays held when confirm is true, writes no file, saves no note, and does not run; daemon CI and apps (Xcode) are both green on `e07856f` (check run `35772919892`); this does not prove a speaker or that a paper was read. Pull request 219 is https://github.com/aryateja2106/lecoder-watch/pull/219 on `cursor/subscription-spoken-ask-e469`, base `cursor/subscription-secret-reply-e469` (still `e07856f2750bcdec4dde54bf3ab772361d6bd998`), tip `fc2a13c5e8ded0a976913b44c0e65f7d2ab6a6aa`; the only added file is `scripts/check-subscription-spoken-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-spoken-ask.sh` and `sh scripts/check-subscription-secret-reply.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a local spoken note is asked through `llm.example`, confirm omitted does not run the command, confirm true runs it once, and a pairing-code ask or transcript stays held; daemon CI and apps (Xcode) are both green on `fc2a13c` (check run `35775132183`); this does not prove a speaker or that a paper was read. Pull request 220 is https://github.com/aryateja2106/lecoder-watch/pull/220 on `cursor/subscription-spoken-secret-reply-e469`, base `cursor/subscription-spoken-ask-e469` (still `fc2a13c5e8ded0a976913b44c0e65f7d2ab6a6aa`), tip `56729000c7117d0430fa710fa9778bdc6da763aa`; the only added file is `scripts/check-subscription-spoken-secret-reply.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-spoken-secret-reply.sh` and `sh scripts/check-subscription-spoken-ask.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a spoken note asked through `llm.example` stays held when the model reply names a pairing code, even when confirm is true, and that reply is not saved; daemon CI and apps (Xcode) are both green on `5672900` (check run `35775998567`); this does not prove a speaker or that a paper was read. Pull request 221 is https://github.com/aryateja2106/lecoder-watch/pull/221 on `cursor/subscription-spoken-saved-ask-e469`, base `cursor/subscription-spoken-secret-reply-e469` (still `56729000c7117d0430fa710fa9778bdc6da763aa`), tip `9fea3cc93645072e61711e569313e6eadbb4a887`; the only added file is `scripts/check-subscription-spoken-saved-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-spoken-saved-ask.sh` and `sh scripts/check-subscription-spoken-secret-reply.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; after a held secret reply, asking only the note titled `spoken-cedar-hinge` through `llm.example` does not run the command when confirm is omitted and runs it once when confirm is true, and the knowledge list has no pairing-code title; daemon CI and apps (Xcode) are both green on `9fea3cc` (check run `35776596930`); this does not prove a speaker or that a paper was read. Pull request 222 is https://github.com/aryateja2106/lecoder-watch/pull/222 on `cursor/extracted-page-speak-e469`, base `cursor/subscription-spoken-saved-ask-e469` (still `9fea3cc93645072e61711e569313e6eadbb4a887`), tip `1f6def342d6fafbd5c15885523e680a494d3b689`; the only added file is `scripts/check-extracted-page-speak.sh`, which does not read `/proc` and on exit kills the daemon process group; `sh scripts/check-extracted-page-speak.sh` and `sh scripts/check-knowledge.sh` exited 0 on `127.0.0.1:8898` and after the new script nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; `POST /knowledge` with a local PDF path and `speak: true` stores the extracted sentence `The oak margin names the extracted page.` under the title `Oak page` and a local `MESH_TTS` binary that exits 0 returns `spoken: true`; a remote `MESH_TTS` is not started and `spoken` is false; omitting `speak` leaves the binary idle; daemon CI and apps (Xcode) are both green on `1f6def3` (check run `35778325457`); this does not prove a speaker or that a paper was read. Pull request 223 is https://github.com/aryateja2106/lecoder-watch/pull/223 on `cursor/extracted-note-ask-e469`, base `cursor/extracted-page-speak-e469` (still `1f6def342d6fafbd5c15885523e680a494d3b689`), tip `3edd7de99a9eceeef4e5b18da6fc42b7ed8a21b5`; the only added file is `scripts/check-extracted-note-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the model stub; `sh scripts/check-extracted-note-ask.sh` and `sh scripts/check-extracted-page-speak.sh` exited 0 on `127.0.0.1:8898` and after the new script nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; `POST /knowledge` with the oak page path and `speak: true` stores `The oak margin names the extracted page.` under the title `Oak page` and a local `MESH_TTS` that exits 0 returns `spoken: true`; asking `Oak page` with `build a reader` does not run when confirm is omitted and runs once when confirm is true; `modelClass` is `user-subscription`; the draft has no `sk-` and no `llm.example` userinfo; asking `Pairing page` with confirm true does not run, does not call the model, and writes no draft; daemon CI is green on `3edd7de` (check run `35781165080`); Apps (Xcode) failed on that run because the model stub port never appeared, so do not claim both jobs are green on `3edd7de`; this does not prove a speaker or that a paper was read. Pull request 224 is https://github.com/aryateja2106/lecoder-watch/pull/224 on `cursor/extracted-note-scope-e469`, base `cursor/extracted-note-ask-e469` (still `3edd7de99a9eceeef4e5b18da6fc42b7ed8a21b5`), tip `2fe3b675b61ed73b6c5cac4858f577b7c7d00f86`; the only added file is `scripts/check-extracted-note-scope.sh`, which does not read `/proc` and on exit kills the daemon process group and the model stub; `sh scripts/check-extracted-note-scope.sh` exited 0 on `127.0.0.1:8898` and after the script nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; one daemon held both the oak page and the pairing page; asking `Oak page` with `build a reader` and confirm true called the model once, ran the command once, and the recorded request contained `The oak margin names the extracted page.` and did not contain `pairing code`; `modelClass` is `user-subscription`; the draft is relative `reader.txt` at mode 600 with no `sk-`, no `llm.example` userinfo, and no `pairing code`; query `page` returned HTTP 409 with error `more than one note` and did not call the model again; daemon CI is green on `2fe3b67` (check run `35782584075`); Apps (Xcode) is still running, so do not claim both jobs are green on `2fe3b67`; this does not prove a speaker or that a paper was read. Pull request 225 is https://github.com/aryateja2106/lecoder-watch/pull/225 on `cursor/extracted-note-local-e469`, base `cursor/extracted-note-scope-e469` (still `2fe3b675b61ed73b6c5cac4858f577b7c7d00f86`), tip `4bd054d1c335e5bbed046ff50046c7cf173787b3`; the only added file is `scripts/check-extracted-note-local.sh`, which does not read `/proc` and on exit kills the daemon process group and the model stub; `sh scripts/check-extracted-note-local.sh` exited 0 on `127.0.0.1:8898` and after the script nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; the model URL was `http://127.0.0.1:<stub-port>/v1` with no userinfo and no `llm.example`; `modelClass` is `local`; one daemon held both the oak page and the pairing page; asking `Oak page` with `build a reader` and confirm true called the model once, ran the command once, and the recorded request contained `The oak margin names the extracted page.` and did not contain `pairing code`; the draft is relative `reader.txt` at mode 600 with no `sk-` and no `pairing code`; query `page` returned HTTP 409 with error `more than one note` and did not call the model again; daemon CI is green on `4bd054d` (check run `35783493628`); Apps (Xcode) is still running, so do not claim both jobs are green on `4bd054d`; this does not prove a speaker or that a paper was read. The model stub on `1ac7d6b554f09b53f357b636ea13a414044d78c2` is written to a file and started with setsid before the wait; `sh scripts/check-extracted-note-local.sh` printed ok and exited 0 on that commit and nothing was listening on port 8898 afterward; the same launch is on `1b349ee88271f77b03d32cf9c6cfac0ff5251129` for pull request 223 and on `fbf7dfabbd0ea1bea4ae24db0f10aa2b8b7c2887` for pull request 224; do not claim Xcode on those three commits; this does not prove a speaker or that a paper was read. Pull request 226 is https://github.com/aryateja2106/lecoder-watch/pull/226 on `cursor/watch-knowledge-ask-e469`, base `cursor/extracted-note-local-e469`, tip `6d04b61a63826054c785014db8c1492f6b5d775f`; the watch ask wires `MeshClient.askAgentNote`, `WatchMeshStore.askCurrentKnowledgeNote`, and `KnowledgeNoteAskView`, and adds `scripts/check-watch-knowledge-ask.sh`; confirm is omitted until the caller passes true; the store returns unless confirmed; the dialog `Ask this note?` calls the store with confirmed true; `sh scripts/check-watch-knowledge-ask.sh` printed OK and exited 0; `sh scripts/check-extracted-note-local.sh` printed ok and exited 0 on this tip; daemon CI is green on `6d04b61` (check run `35785482209`); Apps (Xcode) failed on that run because the model stub port never appeared, so do not claim both jobs are green on `6d04b61`; this does not prove a wrist tap or that a paper was read. Pull request 227 is https://github.com/aryateja2106/lecoder-watch/pull/227 on `cursor/ios-knowledge-ask-e469`, base `cursor/watch-knowledge-ask-e469` (still `6d04b61a63826054c785014db8c1492f6b5d775f`), tip `e8e9da0aace89c2a950054e9a92a2e6966693bab`; the phone ask adds `iOS/ContentView.swift`, `iOS/MeshStore.swift`, and `scripts/check-ios-knowledge-ask.sh`; a reachable machine opens Ask note; the dialog is titled `Ask this note?`; nothing calls `askAgentNote` until the user confirms, then `askCurrentKnowledgeNote` passes confirm true; `sh scripts/check-ios-knowledge-ask.sh` and `sh scripts/check-watch-knowledge-ask.sh` printed OK and exited 0; daemon CI is green on `e8e9da0` (check run `35786627313`); Apps (Xcode) is still running, so do not claim both jobs are green on `e8e9da0`; this does not prove a tap or that a paper was read. Draft PR 223 `91c8683`, PR 224 `aec7ec7`, PR 225 `bda0a60`, PR 226 `1a8576a`, and PR 227 `9edd560` start the extracted-note model stub as a background heredoc and do not call `os.setsid()` on that process; `sh scripts/check-extracted-note-local.sh` printed `check-extracted-note-local: ok` and exited 0 on `bda0a60`; do not claim both jobs are green on those tips. Draft PR 228 `70ba1a5` is that same commit on the phone list. Draft PR 229 `0218729` lists titles on the watch ask screen; a title sets the note field; the dialog stays `Ask this note?`; `sh scripts/check-watch-knowledge-list.sh`, `sh scripts/check-watch-knowledge-ask.sh`, and `sh scripts/check-ios-knowledge-list.sh` each exited 0; do not claim both jobs are green on `0218729`; this does not prove a wrist tap, a speaker, or that a paper was read. Apps (Xcode) failed on `91c8683` (run `35788716439`) because the model stub printed `stub-start` and the port file never appeared within the 15s wait, and Apps (Xcode) failed on `aec7ec7` (run `35788719150`), `bda0a60` (run `35788719875`), `1a8576a` (run `35788724530`), and `9edd560` (run `35788725888`), so do not claim both jobs are green on those tips; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 230 `b16137d` saves a typed note from the phone: POST /knowledge with title and body returns 201 {id, title} and the response omits the body, the note file is mode 600, a whitespace title is 400 and writes no file, GET /knowledge lists the title and not the body, GET /knowledge/:id returns the body, and a pairing code in the body is stored; the save dialog is `Save this note?` and returns unless confirmed; the ask dialog stays `Ask this note?`; `sh scripts/check-typed-knowledge-note.sh`, `sh scripts/check-ios-knowledge-save.sh`, and `sh scripts/check-ios-knowledge-list.sh` each exited 0; daemon CI succeeded on run `35791748067`; do not claim both jobs are green on `b16137d`; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 223 `b6a6e4f`, PR 224 `78278e6`, PR 225 `7f2787b`, PR 226 `63da229`, PR 227 `fc8efec`, PR 228 `2a43190`, PR 229 `ea5a5a9`, and PR 230 `5a9b119` wait 30s for the model stub port after `stub-bind`; `sh scripts/check-extracted-note-local.sh` printed `check-extracted-note-local: ok` and exited 0 on `7f2787b`; do not claim both jobs are green on those tips; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 231 `c739c6e` saves a typed note from the watch: the body field and Save only set a flag; the dialog is `Save this note?` and the store returns unless confirmed; the ask dialog stays `Ask this note?`; `sh scripts/check-watch-knowledge-save.sh`, `sh scripts/check-watch-knowledge-list.sh`, and `sh scripts/check-watch-knowledge-ask.sh` each exited 0; daemon CI succeeded; do not claim both jobs are green on `c739c6e`; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 232 `18c765b` loads a saved note body when a title is tapped: GET /knowledge/:id returns {id, title, body} and the list type stays {id, title}; the body field updates only when the loaded title matches; `sh scripts/check-knowledge-note-body.sh`, `sh scripts/check-ios-knowledge-save.sh`, `sh scripts/check-watch-knowledge-save.sh`, `sh scripts/check-ios-knowledge-list.sh`, and `sh scripts/check-watch-knowledge-list.sh` each exited 0; daemon CI succeeded; do not claim both jobs are green on `18c765b`; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 233 `959020b` searches saved notes from the phone and the watch: an empty query is GET /knowledge, other text is one percent-encoded q using unreserved characters, a query containing :// is not sent, and the list type stays {id, title}; `sh scripts/check-knowledge-note-search.sh`, `sh scripts/check-knowledge-note-body.sh`, `sh scripts/check-ios-knowledge-list.sh`, and `sh scripts/check-watch-knowledge-list.sh` each exited 0; daemon CI succeeded; do not claim both jobs are green on `959020b`; this does not prove a wrist tap, a speaker, or that a paper was read. `sh scripts/check-note-omits-model-url.sh` exited 0 on `127.0.0.1:8898` at `890a704` and `lsof -nP -iTCP:8898 -sTCP:LISTEN` printed nothing afterward. That script no longer reads `/proc`. On exit it signals the daemon process group and the stub. `scripts/check-model-class.sh` was not edited on that commit, so the copy of that script on this branch is still the one that failed macOS CI. Pull request 207 tip `5f6d6b00b32421dcfe015becc378f2771d0eeff6` changes only `scripts/check-model-class.sh`. It no longer reads `/proc`. On exit it signals the daemon process group and the stub. `sh scripts/check-model-class.sh` exited 0, then `sh scripts/check-second-reply-draft.sh` exited 0 on `127.0.0.1:8898`. After each, `lsof -nP -iTCP:8898 -sTCP:LISTEN` printed nothing. A SIGTERM during the model-class check also left 8898 free. Daemon CI and apps (Xcode) are both green on `5f6d6b0` (check run `35759586722`). The failure on `48f1970` stays a failure. This later tip is the fix. Apps (Xcode) failed on `48f1970` (check run `35757430003`). Self-checks failed because `scripts/check-model-class.sh` read `/proc`, which macOS does not have, and port 8898 stayed in use. Daemon CI stayed green. Do not claim both jobs are green on `48f1970`. The fix is not on that tip. This does not prove a speaker or that a paper was read. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- Name the subscription note draft as finished. Pull request 209 adds only `scripts/check-subscription-note.sh`. It does not read `/proc`. On exit it signals the daemon process group and the stub. An allowed ask to `http://llm.example:<stub>/v1` returns `modelClass` `user-subscription`, the stub URL has no userinfo, and the note body is the model reply only. `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited. `sh scripts/check-subscription-note.sh` and `sh scripts/check-note-omits-model-url.sh` on `127.0.0.1:8898` at `9c0f8cf` both exited 0. After the subscription check, port 8898 was free. The gateway key was unset. Daemon CI is green on `9c0f8cf` (check run `35759702609`). Apps (Xcode) is still running on that run. Do not claim both jobs are green on `9c0f8cf`. This tip merges `0a1b0e2`, so it includes the model-class port fix at `5f6d6b0`. Do not claim that Xcode passes. This does not prove a speaker or that a paper was read. Do not add a second `/knowledge` route. Do not call the Vercel AI Gateway.
- State that notes live under `MESHD_STATE` (otherwise `~/.mesh`), in a mode-700 knowledge directory. Knowledge bodies never go to Supabase or to any other cloud store.
- State that speech-out uses a local `MESH_TTS` binary the user already has, and speech-in uses a local `MESH_STT` binary. A remote URL does not run. Do not start a second speech-in path.
- State that Jev chooses a route and does not write the reply. A live gateway call is out of this change. A draft file is not executed. A further command waits for confirm.
- Keep this change to spec files. Do not edit `Shared/Models.swift`, `Shared/MeshClient.swift`, `install/payload/meshd/server.ts`, `install/payload/meshd/pair.ts`, or `project.yml` in this pull request.

No product code lands here. The finished drafts stay the implementation.

## Capabilities

### New Capabilities

- `spoken-knowledge`: Local PDF notes under `MESHD_STATE`, an in-place body replace, optional local TTS and STT binaries, the held app draft that stops for confirm, the agent-note draft of a replaced body, the hold when that body asks to send a pairing code or copy hosts.json, the local-audio replace of an existing note, the spoken-note draft that writes that new body to `held-note.txt`, the local TTS speak of that replaced note, the spoken replace draft that writes that spoken transcript to a held file, the local note search that matches a title or a body and returns only `{ id, title }`, the query-note draft that selects one note with a local `q` when `id` is absent, the local PDF note whose body is stdout from a local `MESH_PDF` executable, the PDF note draft that turns a stub `MESH_PDF` sentence including "summarize this paper" into one note and drafts it when the query has no id, the PDF note speak that returns `spoken: true` only when a local `MESH_TTS` exits 0, the note ask that sends the note, a blank line, and an optional local ask to the model, the spoken note ask that speaks a stub PDF sentence with a local `MESH_TTS` before that ask, the note reply save that stores an allowed ask's model reply as one knowledge note whose title is the ask clipped to 200 characters, the reply note draft that drafts only that saved reply to one relative file, and the saved reply ask that sends that reply, a blank line, and a new local ask, and the second reply draft that drafts only reply B, and the model class draft, and the note that omits the model URL, and the subscription note draft. Knowledge bodies stay on the machine. Nothing from the note is stored in Supabase.
- `local-knowledge-gates`: The finished draft tips, including the note update, the replaced-note draft, the replaced-note hold, the spoken note replace, the spoken-note draft, the spoken replace speak, the spoken replace draft, the local note search, the query-note draft, the local PDF note, the PDF note draft, the PDF note speak, the note ask, the spoken note ask, the note reply save, the reply note draft, the saved reply ask, the second reply draft, the model class draft, the note that omits the model URL, and the subscription note draft, the ban on rebuilding them, the `server.ts` registration rule, the ban on keys and gateway calls, and the rule that agents cannot prove a physical microphone or speaker.

### Modified Capabilities

- None. `terminal-sessions` stays as it is. `openspec/changes/local-brain-and-harness/` stays a spike. Do not merge a harness in this change.

## Non-goals

- Rebuilding local PDF notes (pull request 139), spoken-out via a local `MESH_TTS` binary (pull request 145), the note-to-held-file draft (pull request 154, tip `cba3ac7`), the route-gated draft (pull request 160, tip `4c8a73a`), spoken-in via a local `MESH_STT` binary (pull request 162, tip `4e14cab`), the in-place note replace (pull request 174, tip `426d66d7`), the replaced-note draft (pull request 176, tip `ff4d633c`), the replaced-note hold (pull request 182, tip `56d8ae7`), the spoken note replace (pull request 183, tip `52ce629`), the spoken-note draft (pull request 187, tip `0baf377`), the spoken replace speak (pull request 189, tip `611f321`), the spoken replace draft (pull request 193, tip `37d417aa`), the local note search (pull request 195, tip `1238daabce256e6d46c1fd7dd24680cf810d34f6`), the query-note draft (pull request 197, tip `e11483d4f823ed5a647a59d647566236c8d381ab`), the local PDF note (pull request 198, tip `42211d0ed6531d335fb099ab203a9dc473dacf8c`), the PDF note draft (pull request 199, tip `bd04cc0fe9b56933fd5a23c3b64d6cb654075c2e`), or the PDF note speak (pull request 200, tip `d4486c4c8b6d15255b0e425eba1867f2f22c886c`), the note ask (pull request 201, tip `ae83b9574f098c632a0e31c94105ff3e127e9a43`), the spoken note ask (pull request 202, tip `b4ce6acd1c642720e3d36cdfc15caec673868831`), the note reply save (pull request 203, tip `92d776c9ffa4ed13bea72365bac01d13f25ef672`), the reply note draft (pull request 204, tip `4d6ddbb4bee43456900b3b3c5d4364f7faacac4f`), and the saved reply ask (pull request 205, tip `294d25243d1ac49c56b3b0360ab4c5e1faea1057`). Those drafts are the implementation. Re-check each pull request tip before touching the same files, and do not open a second copy. Pull request 192 (tip `6faf3f3`) already named pull request 189. Pull request 194 (tip `cfe2b17`) already named pull request 193.
- Editing `install/payload/meshd/server.ts` in this pull request. On `origin/main`, `/knowledge` is unregistered. This line registers `handleKnowledge` once. A later agent checks the tree being edited. If `/knowledge` is already registered, that agent says so and does not add a second `/knowledge` route. If it is not, one later task may register the existing handler only when no other agent holds `server.ts`. Pull request 174 and pull request 176 did not edit `server.ts`. Pull request 182 and pull request 183 did not edit `server.ts`. On the spoken-replace tip, `server.ts` still calls `handleKnowledge` once. Pull request 187 did not edit `server.ts`. It adds only `scripts/check-spoken-note-draft.sh`. Pull request 189 did not edit `server.ts`. On that tip, `server.ts` still calls `handleKnowledge` once. Pull request 193 did not edit `server.ts`. It adds only `scripts/check-spoken-replace-draft.sh`. Pull request 195 did not edit `server.ts`. On that tip, `handleKnowledge` is still one call. The files are `install/payload/meshd/knowledge.ts` and `scripts/check-local-note-search.sh`. Pull request 197 did not edit `server.ts`. The files are `install/payload/meshd/agent-note.ts` and `scripts/check-query-note-draft.sh`. Pull request 198 did not edit `server.ts`. The files are `install/payload/meshd/knowledge.ts` and `scripts/check-local-pdf-note.sh`. Pull request 199 did not edit `server.ts`. It adds only `scripts/check-pdf-note-draft.sh`. Pull request 200 did not edit `server.ts`. The files are `install/payload/meshd/knowledge.ts` and `scripts/check-pdf-note-speak.sh`. Pull request 201 did not edit `server.ts`. The files are `install/payload/meshd/agent-note.ts` and `scripts/check-note-ask.sh`. Pull request 202 did not edit `server.ts`. The new commit adds only `scripts/check-spoken-note-ask.sh` after merging pull request 201. Pull request 203 did not edit `server.ts`. The files are `install/payload/meshd/agent-note.ts`, `install/payload/meshd/knowledge.ts`, `scripts/check-note-reply-save.sh`, and `scripts/check-spoken-note-ask.sh`. Pull request 204 did not edit `server.ts`. The only file is `scripts/check-reply-note-draft.sh`. Pull request 205 did not edit `server.ts`. The only file is `scripts/check-saved-reply-ask.sh`. Pull request 206 did not edit `server.ts`. `agent-note.ts` was not edited. The only file is `scripts/check-second-reply-draft.sh`. Pull request 207 did not edit `server.ts`. `agent-note.ts` was not edited. The only file is `scripts/check-model-class.sh`. Pull request 208 did not edit `server.ts`. `agent-note.ts` and `knowledge.ts` were not edited. The only file is `scripts/check-note-omits-model-url.sh`. Pull request 209 did not edit `server.ts`. `agent-note.ts` and `knowledge.ts` were not edited. The only file is `scripts/check-subscription-note.sh`.
- Calling the Vercel AI Gateway, including `POST https://ai-gateway.vercel.sh/v1/evaluate`. Jev chooses a route. It does not write the reply.
- Committing an API key, a JWT, or a database URL. Knowledge bodies never go to Supabase.
- Starting a second speech-in path beside pull request 162. A remote URL does not run.
- Merging the local-brain harness. That proposal stays a spike. Do not replace `meshd`.
- Editing `Shared/Models.swift`, `Shared/MeshClient.swift`, `project.yml`, or `pair.ts`.
- Physical proof that a microphone or speaker works. Agents cannot do that. A spoken note is a local binary transcript or a local TTS exit code. Pull request 187, pull request 189, pull request 193, pull request 195, pull request 197, pull request 198, pull request 199, pull request 200, pull request 201, and pull request 202 are not a microphone proof, a speaker proof, or proof that a paper was read. Pull request 200 does not prove a speaker played audio. Pull request 202 does not prove a speaker played audio or that a paper was read. Pull request 203 does not prove a speaker or that a paper was read. Pull request 204 does not prove a speaker or that a paper was read. Pull request 205 does not prove a speaker or that a paper was read. Pull request 206 does not prove a speaker or that a paper was read. Pull request 207 does not prove a speaker or that a paper was read. Pull request 208 does not prove a speaker or that a paper was read. Pull request 209 does not prove a speaker or that a paper was read. Daemon CI is green on `9c0f8cf` (check run `35759702609`). Apps (Xcode) is still running on that run. Do not claim both jobs are green on `9c0f8cf`. This tip merges `0a1b0e2`, so it includes the model-class port fix at `5f6d6b0`. Do not claim that Xcode passes. Daemon CI is green on `890a704` (check run `35759364727`). Apps (Xcode) is still running on that run. Do not claim both jobs are green on `890a704`. Pull request 208 tip is now `0a1b0e2cd13348983ec20954a9590259df58347c`. It merges `5f6d6b0` into the note-URL branch. No conflicts. History was not rewritten. `sh scripts/check-note-omits-model-url.sh` and `sh scripts/check-model-class.sh` exited 0 on `127.0.0.1:8898`. After each, `lsof -nP -iTCP:8898 -sTCP:LISTEN` printed nothing. Daemon CI and apps (Xcode) are both green on `0a1b0e2` (check run `35759941279`). Pull request 209 tip is now `bbff8dae533cf0373523d28935e758b3101419b7`. It merges `0a1b0e2` into the subscription branch. No conflicts. History was not rewritten. `sh scripts/check-subscription-note.sh` and `sh scripts/check-model-class.sh` exited 0 on `127.0.0.1:8898`. After each, nothing was listening on port 8898. Daemon CI and apps (Xcode) are both green on `bbff8da` (check run `35760223545`). Pull request 210 is https://github.com/aryateja2106/lecoder-watch/pull/210 on `cursor/subscription-reply-draft-e469`, base `cursor/subscription-note-draft-e469` (still `bbff8dae533cf0373523d28935e758b3101419b7`), tip `10da6b70c980660da264e19126b5272eef0c3de3`; the only added file is `scripts/check-subscription-reply.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-reply.sh` and `sh scripts/check-subscription-note.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; daemon CI and apps (Xcode) are both green on `10da6b7` (check run `35761104930`); this does not prove a speaker or that a paper was read. Pull request 211 is https://github.com/aryateja2106/lecoder-watch/pull/211 on `cursor/subscription-reply-ask-e469`, base `cursor/subscription-reply-draft-e469` (still `10da6b70c980660da264e19126b5272eef0c3de3`), tip `785bb6f9c33df2f28d79339360b2a073599317fa`; the only added file is `scripts/check-subscription-reply-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-reply-ask.sh` and `sh scripts/check-subscription-reply.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; daemon CI and apps (Xcode) are both green on `785bb6f` (check run `35762574523`); this does not prove a speaker or that a paper was read. Pull request 212 is https://github.com/aryateja2106/lecoder-watch/pull/212 on `cursor/subscription-second-reply-e469`, base `cursor/subscription-reply-ask-e469` (still `785bb6f9c33df2f28d79339360b2a073599317fa`), tip `29d68882b05952f238206e68ff9fd1c5a858d3c7`; the only added file is `scripts/check-subscription-second-reply.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-second-reply.sh` and `sh scripts/check-subscription-reply-ask.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; daemon CI and apps (Xcode) are both green on `29d6888` (check run `35763737894`); this does not prove a speaker or that a paper was read. Pull request 213 is https://github.com/aryateja2106/lecoder-watch/pull/213 on `cursor/subscription-reply-confirm-e469`, base `cursor/subscription-second-reply-e469` (still `29d68882b05952f238206e68ff9fd1c5a858d3c7`), tip `e424ae27f6aa08a41ae09d0f32e3b69d788ee82e`; the only added file is `scripts/check-subscription-reply-confirm.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-reply-confirm.sh` and `sh scripts/check-subscription-second-reply.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; daemon CI and apps (Xcode) are both green on `e424ae27` (check run `35765862595`); this does not prove a speaker or that a paper was read. Pull request 214 is https://github.com/aryateja2106/lecoder-watch/pull/214 on `cursor/subscription-confirm-draft-e469`, base `cursor/subscription-reply-confirm-e469` (still `e424ae27f6aa08a41ae09d0f32e3b69d788ee82e`), tip `a6bbd0205682d33c40a5b5900ba8567bbe8f9e92` (parent `7dbdbcb942fac63183aba3cc4bb764e7bf325ef0`); the only added file versus the confirm tip is `scripts/check-subscription-confirm-draft.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-confirm-draft.sh` and `sh scripts/check-subscription-reply-confirm.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; the later query is `margin-token-only` and matches only that confirmed ask; daemon CI and apps (Xcode) are both green on `a6bbd02` (check run `35768647989`); this does not prove a speaker or that a paper was read. Pull request 215 is https://github.com/aryateja2106/lecoder-watch/pull/215 on `cursor/subscription-confirm-ask-e469`, base `cursor/subscription-confirm-draft-e469` (still `a6bbd0205682d33c40a5b5900ba8567bbe8f9e92`), tip `5f2e35fabf971041c385126251908c4a98f90794`; the only added file is `scripts/check-subscription-confirm-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-confirm-ask.sh` and `sh scripts/check-subscription-confirm-draft.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a later ask `margin-ask-only` of only the confirmed note does not run the command, and `margin-ask-true` runs it only when confirm is true; daemon CI and apps (Xcode) are both green on `5f2e35f` (check run `35770492947`); this does not prove a speaker or that a paper was read. Pull request 216 is https://github.com/aryateja2106/lecoder-watch/pull/216 on `cursor/subscription-ask-draft-e469`, base `cursor/subscription-confirm-ask-e469` (still `5f2e35fabf971041c385126251908c4a98f90794`), tip `b584480daf55c67070aa96a202b4f8138f2018d5`; the only added file is `scripts/check-subscription-ask-draft.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-ask-draft.sh` and `sh scripts/check-subscription-confirm-ask.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a later query of only the note titled `margin-ask-only` does not run the command, and confirm true creates `held-saved-ran`; daemon CI and apps (Xcode) are both green on `b584480` (check run `35771113323`); this does not prove a speaker or that a paper was read. Pull request 217 is https://github.com/aryateja2106/lecoder-watch/pull/217 on `cursor/subscription-saved-ask-e469`, base `cursor/subscription-ask-draft-e469` (still `b584480daf55c67070aa96a202b4f8138f2018d5`), tip `fa36116ad9aad19b40e50e1afe503e517de980c1`; the only added file is `scripts/check-subscription-saved-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-saved-ask.sh` and `sh scripts/check-subscription-ask-draft.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a further ask `margin-saved-ask` of only the saved note does not run the command, and `margin-saved-true` runs it only when confirm is true; daemon CI and apps (Xcode) are both green on `fa36116` (check run `35771883445`); this does not prove a speaker or that a paper was read. Pull request 218 is https://github.com/aryateja2106/lecoder-watch/pull/218 on `cursor/subscription-secret-reply-e469`, base `cursor/subscription-saved-ask-e469` (still `fa36116ad9aad19b40e50e1afe503e517de980c1`), tip `e07856f2750bcdec4dde54bf3ab772361d6bd998`; the only added file is `scripts/check-subscription-secret-reply.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-secret-reply.sh` and `sh scripts/check-subscription-saved-ask.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a user-subscription reply that names a pairing code stays held when confirm is true, writes no file, saves no note, and does not run; daemon CI and apps (Xcode) are both green on `e07856f` (check run `35772919892`); this does not prove a speaker or that a paper was read. Pull request 219 is https://github.com/aryateja2106/lecoder-watch/pull/219 on `cursor/subscription-spoken-ask-e469`, base `cursor/subscription-secret-reply-e469` (still `e07856f2750bcdec4dde54bf3ab772361d6bd998`), tip `fc2a13c5e8ded0a976913b44c0e65f7d2ab6a6aa`; the only added file is `scripts/check-subscription-spoken-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-spoken-ask.sh` and `sh scripts/check-subscription-secret-reply.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a local spoken note is asked through `llm.example`, confirm omitted does not run the command, confirm true runs it once, and a pairing-code ask or transcript stays held; daemon CI and apps (Xcode) are both green on `fc2a13c` (check run `35775132183`); this does not prove a speaker or that a paper was read. Pull request 220 is https://github.com/aryateja2106/lecoder-watch/pull/220 on `cursor/subscription-spoken-secret-reply-e469`, base `cursor/subscription-spoken-ask-e469` (still `fc2a13c5e8ded0a976913b44c0e65f7d2ab6a6aa`), tip `56729000c7117d0430fa710fa9778bdc6da763aa`; the only added file is `scripts/check-subscription-spoken-secret-reply.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-spoken-secret-reply.sh` and `sh scripts/check-subscription-spoken-ask.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts` and `knowledge.ts` were not edited; a spoken note asked through `llm.example` stays held when the model reply names a pairing code, even when confirm is true, and that reply is not saved; daemon CI and apps (Xcode) are both green on `5672900` (check run `35775998567`); this does not prove a speaker or that a paper was read. Pull request 221 is https://github.com/aryateja2106/lecoder-watch/pull/221 on `cursor/subscription-spoken-saved-ask-e469`, base `cursor/subscription-spoken-secret-reply-e469` (still `56729000c7117d0430fa710fa9778bdc6da763aa`), tip `9fea3cc93645072e61711e569313e6eadbb4a887`; the only added file is `scripts/check-subscription-spoken-saved-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the stub; `sh scripts/check-subscription-spoken-saved-ask.sh` and `sh scripts/check-subscription-spoken-secret-reply.sh` exited 0 on `127.0.0.1:8898` and after each nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; after a held secret reply, asking only the note titled `spoken-cedar-hinge` through `llm.example` does not run the command when confirm is omitted and runs it once when confirm is true, and the knowledge list has no pairing-code title; daemon CI and apps (Xcode) are both green on `9fea3cc` (check run `35776596930`); this does not prove a speaker or that a paper was read. Pull request 222 is https://github.com/aryateja2106/lecoder-watch/pull/222 on `cursor/extracted-page-speak-e469`, base `cursor/subscription-spoken-saved-ask-e469` (still `9fea3cc93645072e61711e569313e6eadbb4a887`), tip `1f6def342d6fafbd5c15885523e680a494d3b689`; the only added file is `scripts/check-extracted-page-speak.sh`, which does not read `/proc` and on exit kills the daemon process group; `sh scripts/check-extracted-page-speak.sh` and `sh scripts/check-knowledge.sh` exited 0 on `127.0.0.1:8898` and after the new script nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; `POST /knowledge` with a local PDF path and `speak: true` stores the extracted sentence `The oak margin names the extracted page.` under the title `Oak page` and a local `MESH_TTS` binary that exits 0 returns `spoken: true`; a remote `MESH_TTS` is not started and `spoken` is false; omitting `speak` leaves the binary idle; daemon CI and apps (Xcode) are both green on `1f6def3` (check run `35778325457`); this does not prove a speaker or that a paper was read. Pull request 223 is https://github.com/aryateja2106/lecoder-watch/pull/223 on `cursor/extracted-note-ask-e469`, base `cursor/extracted-page-speak-e469` (still `1f6def342d6fafbd5c15885523e680a494d3b689`), tip `3edd7de99a9eceeef4e5b18da6fc42b7ed8a21b5`; the only added file is `scripts/check-extracted-note-ask.sh`, which does not read `/proc` and on exit kills the daemon process group and the model stub; `sh scripts/check-extracted-note-ask.sh` and `sh scripts/check-extracted-page-speak.sh` exited 0 on `127.0.0.1:8898` and after the new script nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; `POST /knowledge` with the oak page path and `speak: true` stores `The oak margin names the extracted page.` under the title `Oak page` and a local `MESH_TTS` that exits 0 returns `spoken: true`; asking `Oak page` with `build a reader` does not run when confirm is omitted and runs once when confirm is true; `modelClass` is `user-subscription`; the draft has no `sk-` and no `llm.example` userinfo; asking `Pairing page` with confirm true does not run, does not call the model, and writes no draft; daemon CI is green on `3edd7de` (check run `35781165080`); Apps (Xcode) failed on that run because the model stub port never appeared, so do not claim both jobs are green on `3edd7de`; this does not prove a speaker or that a paper was read. Pull request 224 is https://github.com/aryateja2106/lecoder-watch/pull/224 on `cursor/extracted-note-scope-e469`, base `cursor/extracted-note-ask-e469` (still `3edd7de99a9eceeef4e5b18da6fc42b7ed8a21b5`), tip `2fe3b675b61ed73b6c5cac4858f577b7c7d00f86`; the only added file is `scripts/check-extracted-note-scope.sh`, which does not read `/proc` and on exit kills the daemon process group and the model stub; `sh scripts/check-extracted-note-scope.sh` exited 0 on `127.0.0.1:8898` and after the script nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; one daemon held both the oak page and the pairing page; asking `Oak page` with `build a reader` and confirm true called the model once, ran the command once, and the recorded request contained `The oak margin names the extracted page.` and did not contain `pairing code`; `modelClass` is `user-subscription`; the draft is relative `reader.txt` at mode 600 with no `sk-`, no `llm.example` userinfo, and no `pairing code`; query `page` returned HTTP 409 with error `more than one note` and did not call the model again; daemon CI is green on `2fe3b67` (check run `35782584075`); Apps (Xcode) is still running, so do not claim both jobs are green on `2fe3b67`; this does not prove a speaker or that a paper was read. Pull request 225 is https://github.com/aryateja2106/lecoder-watch/pull/225 on `cursor/extracted-note-local-e469`, base `cursor/extracted-note-scope-e469` (still `2fe3b675b61ed73b6c5cac4858f577b7c7d00f86`), tip `4bd054d1c335e5bbed046ff50046c7cf173787b3`; the only added file is `scripts/check-extracted-note-local.sh`, which does not read `/proc` and on exit kills the daemon process group and the model stub; `sh scripts/check-extracted-note-local.sh` exited 0 on `127.0.0.1:8898` and after the script nothing was listening on port 8898; `agent-note.ts`, `knowledge.ts`, and `server.ts` were not edited; the model URL was `http://127.0.0.1:<stub-port>/v1` with no userinfo and no `llm.example`; `modelClass` is `local`; one daemon held both the oak page and the pairing page; asking `Oak page` with `build a reader` and confirm true called the model once, ran the command once, and the recorded request contained `The oak margin names the extracted page.` and did not contain `pairing code`; the draft is relative `reader.txt` at mode 600 with no `sk-` and no `pairing code`; query `page` returned HTTP 409 with error `more than one note` and did not call the model again; daemon CI is green on `4bd054d` (check run `35783493628`); Apps (Xcode) is still running, so do not claim both jobs are green on `4bd054d`; this does not prove a speaker or that a paper was read. The model stub on `1ac7d6b554f09b53f357b636ea13a414044d78c2` is written to a file and started with setsid before the wait; `sh scripts/check-extracted-note-local.sh` printed ok and exited 0 on that commit and nothing was listening on port 8898 afterward; the same launch is on `1b349ee88271f77b03d32cf9c6cfac0ff5251129` for pull request 223 and on `fbf7dfabbd0ea1bea4ae24db0f10aa2b8b7c2887` for pull request 224; do not claim Xcode on those three commits; this does not prove a speaker or that a paper was read. Pull request 226 is https://github.com/aryateja2106/lecoder-watch/pull/226 on `cursor/watch-knowledge-ask-e469`, base `cursor/extracted-note-local-e469`, tip `6d04b61a63826054c785014db8c1492f6b5d775f`; the watch ask wires `MeshClient.askAgentNote`, `WatchMeshStore.askCurrentKnowledgeNote`, and `KnowledgeNoteAskView`, and adds `scripts/check-watch-knowledge-ask.sh`; confirm is omitted until the caller passes true; the store returns unless confirmed; the dialog `Ask this note?` calls the store with confirmed true; `sh scripts/check-watch-knowledge-ask.sh` printed OK and exited 0; `sh scripts/check-extracted-note-local.sh` printed ok and exited 0 on this tip; daemon CI is green on `6d04b61` (check run `35785482209`); Apps (Xcode) failed on that run because the model stub port never appeared, so do not claim both jobs are green on `6d04b61`; this does not prove a wrist tap or that a paper was read. Pull request 227 is https://github.com/aryateja2106/lecoder-watch/pull/227 on `cursor/ios-knowledge-ask-e469`, base `cursor/watch-knowledge-ask-e469` (still `6d04b61a63826054c785014db8c1492f6b5d775f`), tip `e8e9da0aace89c2a950054e9a92a2e6966693bab`; the phone ask adds `iOS/ContentView.swift`, `iOS/MeshStore.swift`, and `scripts/check-ios-knowledge-ask.sh`; a reachable machine opens Ask note; the dialog is titled `Ask this note?`; nothing calls `askAgentNote` until the user confirms, then `askCurrentKnowledgeNote` passes confirm true; `sh scripts/check-ios-knowledge-ask.sh` and `sh scripts/check-watch-knowledge-ask.sh` printed OK and exited 0; daemon CI is green on `e8e9da0` (check run `35786627313`); Apps (Xcode) is still running, so do not claim both jobs are green on `e8e9da0`; this does not prove a tap or that a paper was read. Draft PR 223 `91c8683`, PR 224 `aec7ec7`, PR 225 `bda0a60`, PR 226 `1a8576a`, and PR 227 `9edd560` start the extracted-note model stub as a background heredoc and do not call `os.setsid()` on that process; `sh scripts/check-extracted-note-local.sh` printed `check-extracted-note-local: ok` and exited 0 on `bda0a60`; do not claim both jobs are green on those tips. Draft PR 228 `70ba1a5` is that same commit on the phone list. Draft PR 229 `0218729` lists titles on the watch ask screen; a title sets the note field; the dialog stays `Ask this note?`; `sh scripts/check-watch-knowledge-list.sh`, `sh scripts/check-watch-knowledge-ask.sh`, and `sh scripts/check-ios-knowledge-list.sh` each exited 0; do not claim both jobs are green on `0218729`; this does not prove a wrist tap, a speaker, or that a paper was read. Apps (Xcode) failed on `91c8683` (run `35788716439`) because the model stub printed `stub-start` and the port file never appeared within the 15s wait, and Apps (Xcode) failed on `aec7ec7` (run `35788719150`), `bda0a60` (run `35788719875`), `1a8576a` (run `35788724530`), and `9edd560` (run `35788725888`), so do not claim both jobs are green on those tips; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 230 `b16137d` saves a typed note from the phone: POST /knowledge with title and body returns 201 {id, title} and the response omits the body, the note file is mode 600, a whitespace title is 400 and writes no file, GET /knowledge lists the title and not the body, GET /knowledge/:id returns the body, and a pairing code in the body is stored; the save dialog is `Save this note?` and returns unless confirmed; the ask dialog stays `Ask this note?`; `sh scripts/check-typed-knowledge-note.sh`, `sh scripts/check-ios-knowledge-save.sh`, and `sh scripts/check-ios-knowledge-list.sh` each exited 0; daemon CI succeeded on run `35791748067`; do not claim both jobs are green on `b16137d`; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 223 `b6a6e4f`, PR 224 `78278e6`, PR 225 `7f2787b`, PR 226 `63da229`, PR 227 `fc8efec`, PR 228 `2a43190`, PR 229 `ea5a5a9`, and PR 230 `5a9b119` wait 30s for the model stub port after `stub-bind`; `sh scripts/check-extracted-note-local.sh` printed `check-extracted-note-local: ok` and exited 0 on `7f2787b`; do not claim both jobs are green on those tips; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 231 `c739c6e` saves a typed note from the watch: the body field and Save only set a flag; the dialog is `Save this note?` and the store returns unless confirmed; the ask dialog stays `Ask this note?`; `sh scripts/check-watch-knowledge-save.sh`, `sh scripts/check-watch-knowledge-list.sh`, and `sh scripts/check-watch-knowledge-ask.sh` each exited 0; daemon CI succeeded; do not claim both jobs are green on `c739c6e`; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 232 `18c765b` loads a saved note body when a title is tapped: GET /knowledge/:id returns {id, title, body} and the list type stays {id, title}; the body field updates only when the loaded title matches; `sh scripts/check-knowledge-note-body.sh`, `sh scripts/check-ios-knowledge-save.sh`, `sh scripts/check-watch-knowledge-save.sh`, `sh scripts/check-ios-knowledge-list.sh`, and `sh scripts/check-watch-knowledge-list.sh` each exited 0; daemon CI succeeded; do not claim both jobs are green on `18c765b`; this does not prove a wrist tap, a speaker, or that a paper was read. Draft PR 233 `959020b` searches saved notes from the phone and the watch: an empty query is GET /knowledge, other text is one percent-encoded q using unreserved characters, a query containing :// is not sent, and the list type stays {id, title}; `sh scripts/check-knowledge-note-search.sh`, `sh scripts/check-knowledge-note-body.sh`, `sh scripts/check-ios-knowledge-list.sh`, and `sh scripts/check-watch-knowledge-list.sh` each exited 0; daemon CI succeeded; do not claim both jobs are green on `959020b`; this does not prove a wrist tap, a speaker, or that a paper was read. `sh scripts/check-note-omits-model-url.sh` exited 0 on `127.0.0.1:8898` at `890a704` and `lsof -nP -iTCP:8898 -sTCP:LISTEN` printed nothing afterward. That script no longer reads `/proc`. On exit it signals the daemon process group and the stub. `scripts/check-model-class.sh` was not edited on that commit, so the copy of that script on this branch is still the one that failed macOS CI. Pull request 207 tip `5f6d6b00b32421dcfe015becc378f2771d0eeff6` changes only `scripts/check-model-class.sh`. It no longer reads `/proc`. On exit it signals the daemon process group and the stub. `sh scripts/check-model-class.sh` exited 0, then `sh scripts/check-second-reply-draft.sh` exited 0 on `127.0.0.1:8898`. After each, `lsof -nP -iTCP:8898 -sTCP:LISTEN` printed nothing. A SIGTERM during the model-class check also left 8898 free. Daemon CI and apps (Xcode) are both green on `5f6d6b0` (check run `35759586722`). The failure on `48f1970` stays a failure. This later tip is the fix. Apps (Xcode) failed on `48f1970` (check run `35757430003`). Self-checks failed because `scripts/check-model-class.sh` read `/proc`, which macOS does not have, and port 8898 stayed in use. Daemon CI stayed green. Do not claim both jobs are green on `48f1970`. The fix is not on that tip. Daemon CI and Xcode CI are both green on `0de3ba9` (check run `35754767900`). Daemon CI and Xcode CI are both green on handoff tip `168d08e` (check run `35754719132`). Daemon CI and Xcode CI are both green on handoff tip `1aca048` (check run `35755761670`). Daemon CI and Xcode CI are both green on `294d252` (check run `35753000381`). Daemon CI and Xcode CI are both green on handoff tip `3ad2a7d` (check run `35752461256`). Daemon CI and Xcode CI are both green on `4d6ddbb` (check run `35750995976`). Daemon CI is green on handoff tip `97278aa` (check run `35751643074`). Apps (Xcode) was cancelled on that run. Do not claim both jobs are green on `97278aa`. Daemon CI and Xcode CI are both green on `4d6ddbb` (check run `35750995976`). Do not claim CI on `fb0aab8`. Daemon CI is green on handoff tip `97278aa` (check run `35751643074`). Apps (Xcode) was cancelled on that run. Do not claim both jobs are green on `97278aa`. Daemon CI and Xcode CI are both green on `b4ce6ac` (check run `35748224297`). Daemon CI and Xcode CI are both green on `ae83b95` (check run `35747085140`). Daemon CI and Xcode CI are both green on `92d776c` (check run `35749935885`). Daemon CI and Xcode CI are both green on handoff tip `8fce18d` (check run `35749871537`). Daemon CI and Xcode CI are both green on handoff tip `3ad2a7d` (check run `35752461256`). Daemon CI and Xcode CI are both green on `42211d0` (check run `35742140469`). Daemon CI and Xcode CI are both green on `bd04cc0` (check run `35743446950`). Daemon CI and Xcode CI are both green on `d4486c4` (check run `35746494919`). Daemon CI and Xcode CI are both green on `1238daa` (check run `35736830546`). Daemon CI and Xcode CI are both green on `e11483d` (check run `35739420177`). Daemon CI and Xcode CI are both green on handoff tip `1e78b081f2c64b038008054bedad1fd3a037ec6b` (check run `35740060386`). Daemon CI and Xcode CI are both green on handoff tip `c140ce7bf73c7f979a28d55d9a4784cc723e0027` (check run `35741550431`). Daemon CI and Xcode CI are both green on handoff tip `978bdbe17099174c5ff5f4cb5b401fa7f6555f6c` (check run `35743157250`). Daemon CI and Xcode CI are both green on handoff tip `dcfa77f5894058d1b815a0400c7396b17e356f87` (check run `35744449505`). Daemon CI and Xcode CI are both green on handoff tip `b3e91609e5a8c488dded8cc4d3aeae1e840c1b2e` (check run `35745531307`). Daemon CI and Xcode CI are both green on handoff tip `2aad3fd833efbf725f57c9e3c76fbf470a07d44a` (check run `35746528962`). Daemon CI and Xcode CI are both green on handoff tip `bd904b19dc5dc85752888193ad365c2d3a176c8a` (check run `35747820842`).

## Impact

- This change adds only `openspec/changes/local-knowledge/`.
- Finished drafts stay where they are: local PDF notes on pull request 139 (`cursor/local-knowledge-e469`, tip `9d2827b`), spoken-out on pull request 145 (`cursor/spoken-note-e469`, tip `ea86b77`), note to a held file on pull request 154 (`cursor/note-to-app-e469`, tip `cba3ac7`), route-gated draft on pull request 160 (`cursor/note-route-draft-e469`, tip `4c8a73a`), spoken-in on pull request 162 (`cursor/spoken-note-in-e469`, tip `4e14cab`), the in-place note replace on pull request 174 (`cursor/knowledge-note-update-e469`, tip `426d66d7`), the replaced-note draft on pull request 176 (`cursor/updated-note-draft-e469`, tip `ff4d633c`), the replaced-note hold on pull request 182 (`cursor/updated-note-hold-e469`, tip `56d8ae7`), the spoken note replace on pull request 183 (`cursor/spoken-note-replace-e469`, tip `52ce629`), the spoken-note draft on pull request 187 (`cursor/spoken-note-draft-e469`, tip `0baf377`), the spoken replace speak on pull request 189 (`cursor/spoken-replace-speak-e469`, tip `611f321`), the spoken replace draft on pull request 193 (`cursor/spoken-replace-draft-e469`, tip `37d417aa`), the local note search on pull request 195 (`cursor/local-note-search-e469`, tip `1238daabce256e6d46c1fd7dd24680cf810d34f6`, base `cursor/spoken-replace-draft-e469`), the query-note draft on pull request 197 (`cursor/query-note-draft-e469`, tip `e11483d4f823ed5a647a59d647566236c8d381ab`, base `cursor/local-note-search-e469`), the local PDF note on pull request 198 (`cursor/local-pdf-note-e469`, tip `42211d0ed6531d335fb099ab203a9dc473dacf8c`, base `cursor/query-note-draft-e469`), the PDF note draft on pull request 199 (`cursor/pdf-note-draft-e469`, tip `bd04cc0fe9b56933fd5a23c3b64d6cb654075c2e`, base `cursor/local-pdf-note-e469`), and the PDF note speak on pull request 200 (`cursor/pdf-note-speak-e469`, tip `d4486c4c8b6d15255b0e425eba1867f2f22c886c`, base `cursor/pdf-note-draft-e469`), the note ask on pull request 201 (`cursor/note-ask-draft-e469`, tip `ae83b9574f098c632a0e31c94105ff3e127e9a43`, base `cursor/pdf-note-draft-e469`), the spoken note ask on pull request 202 (`cursor/spoken-note-ask-e469`, tip `b4ce6acd1c642720e3d36cdfc15caec673868831`, base `cursor/pdf-note-speak-e469`), the note reply save on pull request 203 (`cursor/note-reply-save-e469`, tip `92d776c9ffa4ed13bea72365bac01d13f25ef672`, base `cursor/spoken-note-ask-e469`), the reply note draft on pull request 204 (`cursor/reply-note-draft-e469`, tip `4d6ddbb4bee43456900b3b3c5d4364f7faacac4f`, base `cursor/note-reply-save-e469`), and the saved reply ask on pull request 205 (`cursor/saved-reply-ask-e469`, tip `294d25243d1ac49c56b3b0360ab4c5e1faea1057`, base `cursor/reply-note-draft-e469`), and the second reply draft on pull request 206 (`cursor/second-reply-draft-e469`, tip `0de3ba9bf0188635d573a09ba7419651e5781756`, base `cursor/saved-reply-ask-e469`), and the model class draft on pull request 207 (`cursor/model-class-draft-e469`, tip `5f6d6b00b32421dcfe015becc378f2771d0eeff6`, base `cursor/second-reply-draft-e469`), and the note that omits the model URL on pull request 208 (`cursor/note-omits-model-url-e469`, tip `0a1b0e2cd13348983ec20954a9590259df58347c`, base `cursor/model-class-draft-e469`), and the subscription note draft on pull request 209 (`cursor/subscription-note-draft-e469`, tip `bbff8dae533cf0373523d28935e758b3101419b7`, base `cursor/note-omits-model-url-e469`). Do not start another copy of any of them. Pull request 192 (tip `6faf3f3`) already named pull request 189. Pull request 194 (tip `cfe2b17`) already named pull request 193.
- On `origin/main`, `/knowledge` is unregistered. This line registers `handleKnowledge` once. Do not add a second `/knowledge` route. Notes stay on the machine. Nothing from the note is stored in Supabase.
- Account tables, the heartbeat project, and pairing stay untouched. The company does not host the coding model. Jev only picks a route.
