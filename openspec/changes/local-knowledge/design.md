## Context

See proposal.md for why. The spoken knowledge base and the held app draft already have draft implementations on open pull requests. This design does not replace those drafts. It names them so the next agent continues without rebuilding finished work.

| Slice | Where it already lives | Tip verified with `gh` on 2026-09-22 |
|---|---|---|
| Local PDF notes under `MESHD_STATE`, `/knowledge` registered on that tip | PR 139, branch `cursor/local-knowledge-e469` | `9d2827b` (`9d2827bb0a216cc0f0df20da44da804788c3eba3`) |
| Spoken-out via a local `MESH_TTS` binary | PR 145, branch `cursor/spoken-note-e469` | `ea86b77` (`ea86b77baf5e9902087a1aecc9f2bcf181d00bd7`) |
| Note to a held file; missing note does not call the model; unconfirmed command does not run | PR 154, branch `cursor/note-to-app-e469` | `cba3ac7` (`cba3ac7b70643b155807f789d42a0b525fcab8ae`) |
| Route-gated draft: Jev chooses a route, does not write the reply, no live gateway call | PR 160, branch `cursor/note-route-draft-e469` | `4c8a73a` (`4c8a73a485cfb7acd6d4c83d577a2cf19f8ab29c`) |
| Spoken-in via a local `MESH_STT` binary; a remote URL does not run | PR 162, branch `cursor/spoken-note-in-e469` | `4e14cab` (`4e14cab8c9a5edef4f509b7096fcf238f02ffc2d`) |
| `POST /knowledge/:id` replaces that note's body in the same file. The list stays `{ id, title }`. A missing id creates nothing. A remote URL does not write. `server.ts` was not edited | PR 174, branch `cursor/knowledge-note-update-e469` | `426d66d7` (`426d66d7ae016a67b66b809a7cb4a10aaa300460`) |
| After that replace, the agent-note path drafts the new body. The held file is mode 600 and is not executed. A missing id does not call the model. An unconfirmed command stays unrun | PR 176, branch `cursor/updated-note-draft-e469` | `ff4d633c` (`ff4d633cf15c69776a314c79a4c243d39bf5cd31`) |
| A replaced note that asks to send a pairing code or copy hosts.json stays on hold and the model is not called. "summarize this paper" still drafts the new body. No live gateway call | PR 182, branch `cursor/updated-note-hold-e469` | `56d8ae7` (`56d8ae73883ac3276e7be01f16a949a016bb1253`) |
| `POST /knowledge/:id` accepts `{ audio }`. A local `MESH_STT` transcript replaces that note body. The title stays. The list stays `{ id, title }`. A missing id creates nothing. A remote URL does not write. An empty transcript keeps the old body. `server.ts` still calls `handleKnowledge` once. This does not prove a microphone | PR 183, branch `cursor/spoken-note-replace-e469` | `52ce629` (`52ce62929a7c7e3c660943767311ac3c32d36ca8`) |
| Adds only `scripts/check-spoken-note-draft.sh`. A local `MESH_STT` transcript replaces one note and the agent-note path drafts that new body to `held-note.txt`, mode 600, unexecuted. A pairing-code transcript and a hosts.json transcript stay on hold and the loopback model is not called. "summarize this paper" drafts the new body. A missing id does not call the model. A remote audio path does not write. The gateway key is unset. Daemon CI and Xcode CI are both green. This does not prove a microphone or a speaker | PR 187, branch `cursor/spoken-note-draft-e469` | `0baf377` (`0baf37761f4dc6f42af017fb52513de3fb927e39`) |
| Speaks a replaced note with a local TTS binary. `POST /knowledge/:id` accepts `{ audio, speak }`. When `speak` is true, the title plus the new transcript is handed to `MESH_TTS`. `spoken` is true only when that binary is a local file and the process exits 0. `speak: false`, or a missing TTS binary, stores the transcript and leaves `spoken` false. A remote URL, a scheme, or a protocol-relative path for the audio or for `MESH_TTS` returns before `MESH_STT` and before any write. An empty transcript keeps the old body and does not speak. A missing id creates nothing. The title stays. The list stays `{ id, title }`. `server.ts` was not edited. This does not prove a microphone or a speaker. Pull request 192 (tip `6faf3f3`) already named this slice | PR 189, branch `cursor/spoken-replace-speak-e469` | `611f321` (`611f32131b3609580fb888916a263920b4452dc3`) |
| Adds only `scripts/check-spoken-replace-draft.sh`. `speak: true` with local `MESH_STT` and local `MESH_TTS` returns `spoken` true and drafts that new transcript to a held file, mode 600, unexecuted. A pairing-code transcript and a hosts.json transcript stay on hold. "summarize this paper" still drafts. A missing id and a remote audio or TTS path do not write and do not call the model. There is no `liveGatewayCall`. `server.ts` was not edited. This does not prove a microphone or a speaker. Pull request 194 (tip `cfe2b17`) already named this slice | PR 193, branch `cursor/spoken-replace-draft-e469` | `37d417aa` (`37d417aa7b4f1ca8833136bb029f5b64aae611e0`) |
| `GET /knowledge?q=` matches the title or the body, case-insensitive, and returns only `{ id, title }`. A missing or empty `q` returns every note as `{ id, title }`. A remote URL, a scheme, a protocol-relative value, or any string containing `://` is 400 and is not opened. A pairing-code match and a hosts.json match do not return those bodies. `POST /agent-note` still drafts only the matching paper note to one relative file, mode 600, not executed. A command without confirm does not run. `server.ts` was not edited. `handleKnowledge` is still one call. Daemon CI and Xcode CI are both green on `1238daa` (check run `35736830546`). This does not prove a microphone or a speaker | PR 195, branch `cursor/local-note-search-e469`, base `cursor/spoken-replace-draft-e469` | `1238daa` (`1238daabce256e6d46c1fd7dd24680cf810d34f6`) |
| `POST /agent-note` accepts a local `q` when `id` is absent and selects with `listNotes`. One match drafts one relative file, mode 600, not executed. Zero matches is 404. Several matches is 409. A remote or empty `q` is 400. None of those call the model. The hold matches `textMovesSecret` and does not import `route.ts` or `filter.ts`. `id` without `q` still drafts. `server.ts` was not edited. Daemon CI and Xcode CI are both green on `e11483d` (check run `35739420177`). This does not prove a microphone or a speaker | PR 197, branch `cursor/query-note-draft-e469`, base `cursor/local-note-search-e469` | `e11483d` (`e11483d4f823ed5a647a59d647566236c8d381ab`) |
| `POST /knowledge` accepts `{ pdf, title? }`. `MESH_PDF` must be a local executable. Stdout is the note body. A remote value, a value containing `://`, a missing file, a non-executable, empty stdout, or a nonzero exit writes nothing. The list stays `{ id, title }`. `server.ts` was not edited. The proof is a stub executable. A `path` that is a real PDF still uses the in-daemon `extractPdf` scrape. Daemon CI and Xcode CI are both green on `42211d0` (check run `35742140469`). This does not prove a microphone, a speaker, or that a paper was read | PR 198, branch `cursor/local-pdf-note-e469`, base `cursor/query-note-draft-e469` | `42211d0` (`42211d0ed6531d335fb099ab203a9dc473dacf8c`) |
| Adds only `scripts/check-pdf-note-draft.sh`. A stub `MESH_PDF` sentence that includes "summarize this paper" becomes one note. A query with no id drafts it to one relative file, mode 600, not executed. A pairing-code note and a hosts.json note stay on the existing hold and the model is not called. A pdf value containing `://` and a remote `MESH_PDF` write nothing. `server.ts` was not edited. Daemon CI and Xcode CI are both green on `bd04cc0` (check run `35743446950`). This does not prove a microphone, a speaker, or that a paper was read | PR 199, branch `cursor/pdf-note-draft-e469`, base `cursor/local-pdf-note-e469` | `bd04cc0` (`bd04cc0fe9b56933fd5a23c3b64d6cb654075c2e`) |

On `origin/main` at `5579efd` (the base of this change), `/knowledge` is unregistered in `install/payload/meshd/server.ts`. This line registers `handleKnowledge` once. Do not add a second `/knowledge` route.

Pull request 174 and pull request 176 were re-checked with `gh` on 2026-09-22. Both tips matched the hashes above. Daemon CI and Xcode CI were green on both. Neither pull request edited `server.ts`. On pull request 176, `sh scripts/check-updated-note-draft.sh` passed on `127.0.0.1:8898`. Notes stay on the machine. Nothing from the note is stored in Supabase.

Pull request 182 and pull request 183 were re-checked with `gh` on 2026-09-22. Both tips matched the hashes above. On pull request 182, daemon CI and Xcode CI were green, and there was no live gateway call. On pull request 183, daemon CI was green. The first check showed apps (Xcode) still running. A later check of the same run reported apps (Xcode) pass. This handoff records that pass. It does not treat the job as green before that later check. `sh scripts/check-spoken-note-replace.sh` passed on `127.0.0.1:8898`. Neither pull request edited `server.ts`. On the spoken-replace tip, `server.ts` still calls `handleKnowledge` once. The spoken replace does not prove a microphone.

Pull request 187 was re-checked with `gh` on 2026-09-22. The tip matched `0baf37761f4dc6f42af017fb52513de3fb927e39`. The pull request adds only `scripts/check-spoken-note-draft.sh`. It did not edit `server.ts`. `sh scripts/check-spoken-note-draft.sh` passed on `127.0.0.1:8898` with the gateway key unset. Daemon CI and Xcode CI are both green on `0baf377` (run `35713670176`). This does not prove a microphone or a speaker. Pull request 174 stays `426d66d` on `cursor/knowledge-note-update-e469`. Pull request 176 stays `ff4d633` on `cursor/updated-note-draft-e469`. Pull request 182 stays `56d8ae7` on `cursor/updated-note-hold-e469`. Pull request 183 stays `52ce629` on `cursor/spoken-note-replace-e469`. Those four tips still matched.

Pull request 189 was re-checked with `gh` on 2026-09-22. The tip matched `611f32131b3609580fb888916a263920b4452dc3` on `cursor/spoken-replace-speak-e469`. It speaks a replaced note with a local TTS binary. `POST /knowledge/:id` accepts `{ audio, speak }`. When `speak` is true, the title plus the new transcript is handed to `MESH_TTS`. `spoken` is true only when that binary is a local file and the process exits 0. `speak: false`, or a missing TTS binary, stores the transcript and leaves `spoken` false. A remote URL, a scheme, or a protocol-relative path for the audio or for `MESH_TTS` returns before `MESH_STT` and before any write. An empty transcript keeps the old body and does not speak. A missing id creates nothing. The title stays. The list stays `{ id, title }`. `sh scripts/check-spoken-replace-speak.sh` passed on `127.0.0.1:8898`. The pull request did not edit `server.ts`. On that tip, `server.ts` still calls `handleKnowledge` once. Daemon CI and Xcode CI are both green on `611f321` (check run `35714316679`). This does not prove a microphone or a speaker. Do not call the Vercel AI Gateway. Pull request 192 (tip `6faf3f30d32e7d866853f3090e552b3e465ec571`) already named pull request 189.

Pull request 193 was re-checked with `gh` on 2026-09-22. The tip matched `37d417aa7b4f1ca8833136bb029f5b64aae611e0` on `cursor/spoken-replace-draft-e469`. It adds only `scripts/check-spoken-replace-draft.sh`. `speak: true` with local `MESH_STT` and local `MESH_TTS` returns `spoken` true and drafts that new transcript to a held file, mode 600, unexecuted. A pairing-code transcript and a hosts.json transcript stay on hold. "summarize this paper" still drafts. A missing id and a remote audio or TTS path do not write and do not call the model. There is no `liveGatewayCall`. The pull request did not edit `server.ts`. On that tip, `server.ts` still calls `handleKnowledge` once. Daemon CI is green on `37d417aa`. This does not prove a microphone or a speaker. Do not call the Vercel AI Gateway. Pull request 194 (tip `cfe2b179f78938764e3b5d6949d81981b34c50ef`) already named pull request 193. Pull request 174 stays `426d66d` on `cursor/knowledge-note-update-e469`. Pull request 176 stays `ff4d633` on `cursor/updated-note-draft-e469`. Pull request 182 stays `56d8ae7` on `cursor/updated-note-hold-e469`. Pull request 183 stays `52ce629` on `cursor/spoken-note-replace-e469`. Pull request 187 stays `0baf377` on `cursor/spoken-note-draft-e469`. Pull request 189 stays `611f321` on `cursor/spoken-replace-speak-e469`. Those six tips still matched.

Pull request 195 was re-checked with `gh` on 2026-09-22. The tip matched `1238daabce256e6d46c1fd7dd24680cf810d34f6` on `cursor/local-note-search-e469`, base `cursor/spoken-replace-draft-e469`. It is a draft. `GET /knowledge?q=` matches the title or the body, case-insensitive, and returns only `{ id, title }`. A missing or empty `q` returns every note as `{ id, title }`. A remote URL, a scheme, a protocol-relative value, or any string containing `://` is 400 and is not opened. A pairing-code match and a hosts.json match do not return those bodies. `POST /agent-note` still drafts only the matching paper note to one relative file, mode 600, not executed. A command without confirm does not run. The file list is `install/payload/meshd/knowledge.ts` and `scripts/check-local-note-search.sh`. The pull request did not edit `server.ts`. On that tip, `handleKnowledge` is still one call. On `origin/main`, `/knowledge` is unregistered. This line registers `handleKnowledge` once. The coordinator re-ran `sh scripts/check-local-note-search.sh` on `127.0.0.1:8898` at `1238daa` and it exited 0. The gateway key was unset. Daemon CI and Xcode CI are both green on `1238daa` (check run `35736830546`). This does not prove a microphone or a speaker. Do not call the Vercel AI Gateway. Pull request 174 stays `426d66d` on `cursor/knowledge-note-update-e469`. Pull request 176 stays `ff4d633` on `cursor/updated-note-draft-e469`. Pull request 182 stays `56d8ae7` on `cursor/updated-note-hold-e469`. Pull request 183 stays `52ce629` on `cursor/spoken-note-replace-e469`. Pull request 187 stays `0baf377` on `cursor/spoken-note-draft-e469`. Pull request 189 stays `611f321` on `cursor/spoken-replace-speak-e469`. Pull request 193 stays `37d417aa` on `cursor/spoken-replace-draft-e469`. Those seven tips still matched.

Pull request 197 was re-checked with `gh` on 2026-09-22. The tip matched `e11483d4f823ed5a647a59d647566236c8d381ab` on `cursor/query-note-draft-e469`, base `cursor/local-note-search-e469`. It is a draft. `POST /agent-note` accepts a local `q` when `id` is absent and selects with `listNotes`. One match drafts one relative file, mode 600, not executed. Zero matches is 404. Several matches is 409. A remote or empty `q` is 400. None of those call the model. The hold matches `textMovesSecret` (pairing code, hosts.json, mesh token, `.mesh/token`, and the upload/send sentence) and does not import `route.ts` or `filter.ts`. `id` without `q` still drafts. The file list is `install/payload/meshd/agent-note.ts` and `scripts/check-query-note-draft.sh`. The pull request did not edit `server.ts`, `knowledge.ts`, or add a second `/knowledge` route. The coordinator re-ran `bun x tsc --noEmit -p install/payload/meshd/tsconfig.json` and `sh scripts/check-query-note-draft.sh` on `127.0.0.1:8898` at `e11483d`. Both exited 0. The gateway key was unset. Daemon CI and Xcode CI are both green on `e11483d` (check run `35739420177`). Daemon CI and Xcode CI are both green on handoff tip `1e78b081f2c64b038008054bedad1fd3a037ec6b` (check run `35740060386`). Daemon CI and Xcode CI are both green on handoff tip `c140ce7bf73c7f979a28d55d9a4784cc723e0027` (check run `35741550431`). Daemon CI and Xcode CI are both green on handoff tip `978bdbe17099174c5ff5f4cb5b401fa7f6555f6c` (check run `35743157250`). Daemon CI and Xcode CI are both green on handoff tip `dcfa77f5894058d1b815a0400c7396b17e356f87` (check run `35744449505`). Daemon CI and Xcode CI are both green on handoff tip `b3e91609e5a8c488dded8cc4d3aeae1e840c1b2e` (check run `35745531307`). This does not prove a microphone or a speaker. Do not call the Vercel AI Gateway. Pull request 195 stays `1238daa` on `cursor/local-note-search-e469`, with daemon CI and Xcode CI both green (check run `35736830546`). Pull request 174 stays `426d66d`. Pull request 176 stays `ff4d633`. Pull request 182 stays `56d8ae7`. Pull request 183 stays `52ce629`. Pull request 187 stays `0baf377`. Pull request 189 stays `611f321`. Pull request 193 stays `37d417aa`. Those tips still matched.

Pull request 198 was re-checked with `gh` on 2026-09-22. The tip matched `42211d0ed6531d335fb099ab203a9dc473dacf8c` on `cursor/local-pdf-note-e469`, base `cursor/query-note-draft-e469`. It is a draft. `POST /knowledge` accepts `{ pdf, title? }`. `MESH_PDF` must be a local executable. Its stdout is the note body. A remote value, a value containing `://`, a missing file, a non-executable, empty stdout, or a nonzero exit writes nothing. The list stays `{ id, title }`. The file list is `install/payload/meshd/knowledge.ts` and `scripts/check-local-pdf-note.sh`. The pull request did not edit `server.ts`. The coordinator re-ran `sh scripts/check-local-pdf-note.sh` and `sh scripts/check-local-note-search.sh` on `127.0.0.1:8898` at `42211d0`. Both exited 0. The proof is a stub executable. A `path` that is a real PDF still uses the in-daemon `extractPdf` scrape. Do not rebuild either path. Daemon CI and Xcode CI are both green on `42211d0` (check run `35742140469`). This does not prove a microphone, a speaker, or that a paper was read. Do not call the Vercel AI Gateway. Pull request 197 stays `e11483d` on `cursor/query-note-draft-e469`, with daemon CI and Xcode CI both green (check run `35739420177`). Pull request 195 stays `1238daa` on `cursor/local-note-search-e469`, with daemon CI and Xcode CI both green (check run `35736830546`).

Pull request 199 was re-checked with `gh` on 2026-09-22. The tip matched `bd04cc0fe9b56933fd5a23c3b64d6cb654075c2e` on `cursor/pdf-note-draft-e469`, base `cursor/local-pdf-note-e469`. It is a draft. The only file is `scripts/check-pdf-note-draft.sh`. A stub `MESH_PDF` sentence that includes "summarize this paper" becomes one note. A query with no id drafts it to one relative file, mode 600, not executed. A pairing-code note and a hosts.json note stay on the existing hold and the model is not called. A pdf value containing `://` and a remote `MESH_PDF` write nothing. The pull request did not edit `server.ts`. The coordinator re-ran `sh scripts/check-pdf-note-draft.sh` on `127.0.0.1:8898` at `bd04cc0` and it exited 0. Daemon CI and Xcode CI are both green on `bd04cc0` (check run `35743446950`). This does not prove a microphone, a speaker, or that a paper was read. Do not call the Vercel AI Gateway. Pull request 198 stays `42211d0` on `cursor/local-pdf-note-e469`, with daemon CI and Xcode CI both green (check run `35742140469`). Pull request 197 stays `e11483d`, with both CI jobs green (check run `35739420177`). Pull request 195 stays `1238daa`, with both CI jobs green (check run `35736830546`). Daemon CI and Xcode CI are both green on handoff tip `978bdbe17099174c5ff5f4cb5b401fa7f6555f6c` (check run `35743157250`). Daemon CI and Xcode CI are both green on handoff tip `dcfa77f5894058d1b815a0400c7396b17e356f87` (check run `35744449505`). Daemon CI and Xcode CI are both green on handoff tip `b3e91609e5a8c488dded8cc4d3aeae1e840c1b2e` (check run `35745531307`).

`openspec/changes/local-brain-and-harness/` stays a spike. `meshd` stays the session layer. The model is one the user configured. Jev only picks a route. The company does not host the coding model.

Stakeholders: the person who already has a PDF and (optionally) local speech binaries on the machine, and the agent who continues the slice. Arya's physical iPhone, Apple Watch, microphone, and speaker are out of reach for any agent.

## Goals / Non-Goals

**Goals:**

- Give the next agent one contract for notes on disk, local speech handoffs, and the held app draft.
- Keep knowledge bodies under `MESHD_STATE`. Keep them off Supabase and off any other cloud store.
- Keep finished drafts as the implementation, including the in-place note replace, the replaced-note draft, the replaced-note hold, the spoken note replace, the spoken-note draft, the spoken replace speak, the spoken replace draft, the local note search, the query-note draft, the local PDF note via `MESH_PDF`, and the PDF note draft. Re-check each tip before editing the same files.
- Make the `server.ts` rule obvious: this change does not edit it. On `origin/main`, `/knowledge` is unregistered. This line registers `handleKnowledge` once. Do not add a second `/knowledge` route. A later agent registers only when the route is missing and no other agent holds that file.

**Non-Goals:**

- A second copy of pull requests 139, 145, 154, 160, 162, 174, 176, 182, 183, 187, 189, 193, 195, 197, 198, or 199.
- Editing `server.ts`, `pair.ts`, `Shared/Models.swift`, `Shared/MeshClient.swift`, or `project.yml` in this change.
- A live AI Gateway call. Committing an API key, a JWT, or a database URL.
- Merging the local-brain harness. Replacing `meshd`.
- Physical proof of a microphone or speaker.

## Decisions

### 1. Finished drafts are the implementation

PR 139 is the local PDF note and the knowledge module that owns its own file. PR 145 is spoken-out through `MESH_TTS`. PR 154 is the note-to-held-file path. PR 160 is the route-gated draft. PR 162 is spoken-in through `MESH_STT`. PR 174 is the in-place note replace. PR 176 is the replaced-note draft. PR 182 is the replaced-note hold. PR 183 is the spoken note replace. PR 187 is the spoken-note draft. PR 189 is the spoken replace speak. PR 193 is the spoken replace draft. PR 195 is the local note search. PR 197 is the query-note draft. PR 198 is the local PDF note whose body is stdout from a local `MESH_PDF` executable. A `path` that is a real PDF still uses the in-daemon `extractPdf` scrape. Do not rebuild either path. PR 199 is the PDF note draft. Daemon CI and Xcode CI are both green on `42211d0` (check run `35742140469`). Daemon CI and Xcode CI are both green on `bd04cc0` (check run `35743446950`). Pull request 192 (tip `6faf3f3`) already named pull request 189. Pull request 194 (tip `cfe2b17`) already named pull request 193. Pull request 195 stays `1238daa`, with daemon CI and Xcode CI both green (check run `35736830546`). An agent who needs one of those behaviors opens that pull request and re-checks the tip. Do not start another copy of any of them.

Alternative considered: re-implement each slice on `main` inside this change so the spec and the code land together. Rejected. A second copy of a finished draft is the failure mode this handoff exists to prevent.

### 2. Notes stay under `MESHD_STATE`, never in Supabase

The note directory is `$MESHD_STATE/knowledge/` when `MESHD_STATE` is set, otherwise `~/.mesh/knowledge/`. Directory mode 700. File mode 600. Knowledge bodies, PDF bytes, transcripts, and draft text stay on the machine. Nothing from the note is stored in Supabase. Account tables do not store prompts, PDFs, or generated apps.

Alternative considered: upload note titles to the account database for cross-device search. Rejected. That would make the knowledge base a second cloud copy of the person's papers. Task 7 in the Oct 1 plan sequences after sealed device sync for that reason.

### 3. Speech is a local binary the user already has

Spoken-out hands note text to `MESH_TTS` on stdin when the request asks for speech. Spoken-in reads a local audio file through `MESH_STT` and writes the transcript as the note body. A remote URL does not run. No speech program is downloaded. Do not start a second speech-in path beside PR 162.

Alternative considered: a company STT or TTS endpoint. Rejected. The person this is for already has a binary on the machine, or they do not use speech yet.

### 4. Jev chooses a route and does not write the reply

PR 160 posts a filtered note to a local model stub only when `route()` returns `allow-local-tool`. A secret-moving note, a graphical ask, a wait-for-human result, a high risk, or confidence below 0.6 does not connect and does not create the file. The check runs with the gateway key unset. A live gateway call waits on a card on the Vercel team and is outside this change.

Alternative considered: let Jev draft the file text. Rejected. Jev evaluates. The user's model writes. The draft file is not executed. A further command waits for confirm (the existing review-before-dispatch rule).

### 5. This change does not edit `server.ts`

On `origin/main`, `/knowledge` is unregistered. This line registers `handleKnowledge` once. A later agent checks the tree it is editing. If `/knowledge` is already registered, that agent says so and does not add a second `/knowledge` route. If it is not, one later task may register the existing handler only when no other agent holds `server.ts`. This pull request itself contains no `server.ts` edit. Pull request 174 and pull request 176 did not edit `server.ts`. Pull request 182 and pull request 183 did not edit `server.ts`. On the spoken-replace tip, `server.ts` still calls `handleKnowledge` once. Pull request 187 did not edit `server.ts`. It adds only `scripts/check-spoken-note-draft.sh`. Pull request 189 did not edit `server.ts`. On that tip, `server.ts` still calls `handleKnowledge` once. Pull request 193 did not edit `server.ts`. It adds only `scripts/check-spoken-replace-draft.sh`. Pull request 195 did not edit `server.ts`. On that tip, `handleKnowledge` is still one call. Pull request 197 did not edit `server.ts`. Pull request 198 did not edit `server.ts`. The files are `install/payload/meshd/knowledge.ts` and `scripts/check-local-pdf-note.sh`. Pull request 199 did not edit `server.ts`. It adds only `scripts/check-pdf-note-draft.sh`.

Alternative considered: register the route in this change so the next agent has a green path on `main`. Rejected. Shared contracts are serialized. This change is the handoff, not the registration. A second `/knowledge` route on the knowledge branch is the same failure.

### 6. The harness stays a spike

Task 8 in the Oct 1 plan may later point `ctx.subprocess` / `ctx.fs` at existing session routes in a time-boxed adapter beside the daemon. If the upstream preview breaks, stop and write what broke into the proposal's `tasks.md`. Do not replace `meshd`. Do not merge `openspec/changes/local-brain-and-harness/` as the shipping loop.

### 7. The in-place replace and the replaced-note draft are finished

`POST /knowledge/:id` on pull request 174 replaces that note's body in the same file. The list stays `{ id, title }`. A missing id creates nothing. A remote URL does not write. Pull request 176 runs after that replace: the agent-note path drafts the new body. The held file is mode 600 and is not executed. A missing id does not call the model. An unconfirmed command stays unrun. Daemon CI and Xcode CI were green on both pull requests when this handoff was written. The replaced-note check passed on `127.0.0.1:8898`. Notes stay on the machine. Nothing from the note is stored in Supabase.

A spoken note is a local binary transcript or a local TTS exit code. Agents cannot prove a microphone or a speaker.

Alternative considered: write a second replace handler or a second draft check beside these pull requests. Rejected. Those drafts are the implementation.

### 8. The replaced-note hold is finished

Pull request 182 holds a replaced note that asks to send a pairing code or copy hosts.json. The model is not called. There is no live gateway call. "summarize this paper" still drafts the new body. Daemon CI and Xcode CI were green when this handoff was written.

Alternative considered: add another hold beside the route that already refuses a pairing code and hosts.json. Rejected. That hold is the implementation.

### 9. The spoken note replace is finished

Pull request 183 accepts `{ audio }` on `POST /knowledge/:id`. A local `MESH_STT` transcript replaces that note body. The title stays. The list stays `{ id, title }`. A missing id creates nothing. A remote URL does not write. An empty transcript keeps the old body. `server.ts` still calls `handleKnowledge` once. `sh scripts/check-spoken-note-replace.sh` passed on `127.0.0.1:8898`. Daemon CI was green. The first check showed apps (Xcode) still running. A later check of the same run reported apps (Xcode) pass. This handoff records that pass. It does not treat the job as green before that later check. This does not prove a microphone. It is not a second speech-in path and it is not a second `/knowledge` route.

Alternative considered: add a second speech-in route for an existing note. Rejected. Spoken-in for a new note stays on pull request 162. This replace uses the knowledge handler that is already registered once.

### 10. The spoken-note draft is finished

Pull request 187 adds only `scripts/check-spoken-note-draft.sh`. A local `MESH_STT` transcript replaces one note and the agent-note path drafts that new body to `held-note.txt`. That file is mode 600 and is not executed. A pairing-code transcript and a hosts.json transcript stay on hold and the loopback model is not called. "summarize this paper" drafts the new body. A missing id does not call the model. A remote audio path does not write. The check passed on `127.0.0.1:8898` with the gateway key unset. Daemon CI and Xcode CI are both green on `0baf377`. This does not prove a microphone or a speaker. It is not a second `/knowledge` route.

Alternative considered: write another check that replaces a note from audio and then drafts it. Rejected. That check is the implementation.

### 11. The spoken replace speak is finished

Pull request 189 speaks a replaced note with a local TTS binary. `POST /knowledge/:id` accepts `{ audio, speak }`. When `speak` is true, the title plus the new transcript is handed to `MESH_TTS`. `spoken` is true only when that binary is a local file and the process exits 0. `speak: false`, or a missing TTS binary, stores the transcript and leaves `spoken` false. A remote URL, a scheme, or a protocol-relative path for the audio or for `MESH_TTS` returns before `MESH_STT` and before any write. An empty transcript keeps the old body and does not speak. A missing id creates nothing. The title stays. The list stays `{ id, title }`. `sh scripts/check-spoken-replace-speak.sh` passed on `127.0.0.1:8898`. `server.ts` was not edited. On that tip, `server.ts` still calls `handleKnowledge` once. Daemon CI and Xcode CI are both green on `611f321` (check run `35714316679`). This does not prove a microphone or a speaker. It is not a second speech path and it is not a second `/knowledge` route. Do not call the Vercel AI Gateway.

Alternative considered: add another TTS path beside the replace that already speaks the new transcript. Rejected. That speak path is the implementation.

### 12. The spoken replace draft is finished

Pull request 193 adds only `scripts/check-spoken-replace-draft.sh`. `speak: true` with local `MESH_STT` and local `MESH_TTS` returns `spoken` true and drafts that new transcript to a held file. That file is mode 600 and is not executed. A pairing-code transcript and a hosts.json transcript stay on hold. "summarize this paper" still drafts. A missing id and a remote audio or TTS path do not write and do not call the model. There is no `liveGatewayCall`. `server.ts` was not edited. On that tip, `server.ts` still calls `handleKnowledge` once. Daemon CI is green on `37d417aa`. This does not prove a microphone or a speaker. It is not a second `/knowledge` route. Do not call the Vercel AI Gateway. Pull request 192 (tip `6faf3f3`) already named pull request 189. Pull request 194 (tip `cfe2b17`) already named pull request 193.

Alternative considered: write another check that speaks a replaced note and then drafts the new transcript. Rejected. That check is the implementation.

### 13. The local note search is finished

Pull request 195 matches `GET /knowledge?q=` against the title or the body, without case, and returns only `{ id, title }`. A missing or empty `q` returns every note as `{ id, title }`. A remote URL, a scheme, a protocol-relative value, or any string containing `://` is 400 and is not opened. A pairing-code match and a hosts.json match do not return those bodies. `POST /agent-note` still drafts only the matching paper note to one relative file. That file is mode 600 and is not executed. A command without confirm does not run. `server.ts` was not edited. On that tip, `handleKnowledge` is still one call. On `origin/main`, `/knowledge` is unregistered. This line registers `handleKnowledge` once. The coordinator re-ran `sh scripts/check-local-note-search.sh` on `127.0.0.1:8898` at `1238daa` and it exited 0. The gateway key was unset. Daemon CI and Xcode CI are both green on `1238daa` (check run `35736830546`). This does not prove a microphone or a speaker. It is not a second `/knowledge` route. Do not call the Vercel AI Gateway. Pull request 194 (tip `cfe2b17`) already named pull request 193.

Alternative considered: write another search that returns note bodies, or a second `/knowledge` route for the query. Rejected. That search is the implementation.

### 14. The query-note draft is finished

Pull request 197 accepts a local `q` on `POST /agent-note` when `id` is absent and selects with `listNotes`. One match drafts one relative file. That file is mode 600 and is not executed. Zero matches is 404. Several matches is 409. A remote or empty `q` is 400. None of those call the model. The hold matches `textMovesSecret` (pairing code, hosts.json, mesh token, `.mesh/token`, and the upload/send sentence) and does not import `route.ts` or `filter.ts`. `id` without `q` still drafts. `server.ts` was not edited. The coordinator re-ran `bun x tsc --noEmit -p install/payload/meshd/tsconfig.json` and `sh scripts/check-query-note-draft.sh` on `127.0.0.1:8898` at `e11483d`. Both exited 0. The gateway key was unset. Daemon CI and Xcode CI are both green on `e11483d` (check run `35739420177`). Daemon CI and Xcode CI are both green on handoff tip `1e78b081f2c64b038008054bedad1fd3a037ec6b` (check run `35740060386`). Daemon CI and Xcode CI are both green on handoff tip `c140ce7bf73c7f979a28d55d9a4784cc723e0027` (check run `35741550431`). Daemon CI and Xcode CI are both green on handoff tip `978bdbe17099174c5ff5f4cb5b401fa7f6555f6c` (check run `35743157250`). Daemon CI and Xcode CI are both green on handoff tip `dcfa77f5894058d1b815a0400c7396b17e356f87` (check run `35744449505`). Daemon CI and Xcode CI are both green on handoff tip `b3e91609e5a8c488dded8cc4d3aeae1e840c1b2e` (check run `35745531307`). This does not prove a microphone or a speaker. It is not a second `/knowledge` route. Do not call the Vercel AI Gateway. Pull request 195 stays the local note search at `1238daa`, with both CI jobs green (check run `35736830546`).

Alternative considered: import `route.ts` and `filter.ts` for the hold, or guess among several matches. Rejected. The copied `textMovesSecret` hold and the 409 are the implementation.

### 15. The local PDF note is finished

Pull request 198 accepts `{ pdf, title? }` on `POST /knowledge`. `MESH_PDF` must be a local executable. Its stdout is the note body. A remote value, a value containing `://`, a missing file, a non-executable, empty stdout, or a nonzero exit writes nothing. The list stays `{ id, title }`. `server.ts` was not edited. The coordinator re-ran `sh scripts/check-local-pdf-note.sh` and `sh scripts/check-local-note-search.sh` on `127.0.0.1:8898` at `42211d0`. Both exited 0. The proof is a stub executable. A `path` that is a real PDF still uses the in-daemon `extractPdf` scrape. Do not rebuild either path. Daemon CI and Xcode CI are both green on `42211d0` (check run `35742140469`). This does not prove a microphone, a speaker, or that a paper was read. It is not a second `/knowledge` route. Do not call the Vercel AI Gateway. Pull request 197 stays the query-note draft at `e11483d`, with both CI jobs green (check run `35739420177`). Pull request 195 stays the local note search at `1238daa`, with both CI jobs green (check run `35736830546`).

Alternative considered: scrape a `{ pdf }` file with the in-daemon `extractPdf` path, or treat the stub check as proof a paper was read. Rejected. Stdout from a local `MESH_PDF` executable is the note body. The `{ path }` scrape stays `extractPdf`. The stub executable is the proof.

### 16. The PDF note draft is finished

Pull request 199 adds only `scripts/check-pdf-note-draft.sh`. A stub `MESH_PDF` sentence that includes "summarize this paper" becomes one note. A query with no id drafts it to one relative file. That file is mode 600 and is not executed. A pairing-code note and a hosts.json note stay on the existing hold and the model is not called. A pdf value containing `://` and a remote `MESH_PDF` write nothing. `server.ts` was not edited. The coordinator re-ran `sh scripts/check-pdf-note-draft.sh` on `127.0.0.1:8898` at `bd04cc0` and it exited 0. Daemon CI and Xcode CI are both green on `bd04cc0` (check run `35743446950`). This does not prove a microphone, a speaker, or that a paper was read. It is not a second `/knowledge` route. Do not call the Vercel AI Gateway. Pull request 198 stays the local PDF note at `42211d0`, with both CI jobs green (check run `35742140469`). Pull request 197 stays the query-note draft at `e11483d`, with both CI jobs green (check run `35739420177`). Pull request 195 stays the local note search at `1238daa`, with both CI jobs green (check run `35736830546`).

Alternative considered: write another check that ingests a paper sentence and then drafts it. Rejected. That check is the implementation.

## Risks / Trade-offs

- [An agent rebuilds PR 139, 145, 154, 160, 162, 174, 176, 182, 183, 187, 189, 193, 195, 197, 198, or 199] → The tasks point at the existing pull request, record the current `headRefOid`, and stop. A new file for one of those slices is a failed task. A `{ pdf }` note and a `{ path }` PDF both already exist. Do not rebuild either path. The PDF note draft already exists. Do not rebuild that check.
- [An agent registers `/knowledge` a second time because PR 139 is not merged] → On `origin/main`, `/knowledge` is unregistered. This line registers `handleKnowledge` once. The later agent first checks whether the route is already present. If it is, do not add a second `/knowledge` route. If it is not, register only when no other agent holds `server.ts`.
- [Knowledge bodies land in Supabase, logs, or a gateway payload] → The specs treat that as a failed design. The checks on the finished drafts already refuse a Supabase client and a gateway call in the knowledge and draft paths.
- [A draft file is treated as an executed app] → The held-file and route-gated drafts stop for confirm. An unconfirmed command does not run.
- [An agent claims the microphone or speaker works, or that a paper was read] → No task requires Arya's physical devices. Agents cannot prove a physical microphone or speaker. A spoken note is a local binary transcript or a local TTS exit code. Pull request 187, pull request 189, pull request 193, pull request 195, pull request 197, pull request 198, and pull request 199 are not that proof. The local PDF note proof is a stub executable. Say so and hand a physical check back to a human.
- [An agent treats green CI on pull request 197 as a microphone or a speaker] → Daemon CI and Xcode CI are both green on `e11483d` (check run `35739420177`). Daemon CI and Xcode CI are both green on handoff tip `1e78b081f2c64b038008054bedad1fd3a037ec6b` (check run `35740060386`). Daemon CI and Xcode CI are both green on handoff tip `c140ce7bf73c7f979a28d55d9a4784cc723e0027` (check run `35741550431`). Daemon CI and Xcode CI are both green on handoff tip `978bdbe17099174c5ff5f4cb5b401fa7f6555f6c` (check run `35743157250`). Daemon CI and Xcode CI are both green on handoff tip `dcfa77f5894058d1b815a0400c7396b17e356f87` (check run `35744449505`). Daemon CI and Xcode CI are both green on handoff tip `b3e91609e5a8c488dded8cc4d3aeae1e840c1b2e` (check run `35745531307`). That is not a microphone proof and not a speaker proof.
- [An agent treats green CI on pull request 198 as a microphone, a speaker, or proof that a paper was read] → Daemon CI and Xcode CI are both green on `42211d0` (check run `35742140469`). The proof is a stub executable. That is not a microphone proof, a speaker proof, or proof that a paper was read.
- [An agent treats green CI on pull request 199 as a microphone, a speaker, or proof that a paper was read] → Daemon CI and Xcode CI are both green on `bd04cc0` (check run `35743446950`). Daemon CI and Xcode CI are both green on handoff tip `dcfa77f5894058d1b815a0400c7396b17e356f87` (check run `35744449505`). Daemon CI and Xcode CI are both green on handoff tip `b3e91609e5a8c488dded8cc4d3aeae1e840c1b2e` (check run `35745531307`). That is not a microphone proof, a speaker proof, or proof that a paper was read.
- [An agent treats green CI on pull request 195 as a microphone or a speaker] → Daemon CI and Xcode CI are both green on `1238daa` (check run `35736830546`). That is not a microphone proof and not a speaker proof.
- [A key, JWT, or database URL lands in the diff] → The proof command searches the change and must find none. Do not call the AI Gateway.
- [Someone merges the local-brain harness as the shipping loop] → Out of scope. The harness proposal stays a spike.

## Migration Plan

This change deploys nothing. Merging it adds the spec under `openspec/changes/local-knowledge/` and leaves the daemon, the account tables, and production as they are.

Rollback is reverting that commit. There is no schema migration, no env var, and no feature flag in this change.

A later agent continues from the finished draft tips above. That later work is not a reason to rebuild those drafts inside this change.

## Open Questions

None that block this handoff. Product decisions about Pro unlocking the spoken knowledge base stay with Ritik and do not change these requirements.
