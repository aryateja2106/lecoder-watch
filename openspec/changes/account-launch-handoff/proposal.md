## Why

Shipping an account that looks like a copy of the mesh would be a broken launch. The owner's words for the boundary are: "The account is identity. It is not a copy of `~/.mesh`." and "Pretending the phone is synced is a broken launch."

The person this is for can type an email, a username, and a password. They do not configure SSH keys, port forwarding, a VPN, or a database. Ritik is the person who confirms the new Supabase project. An executing agent does not stand in for either of them.

## What Changes

- Add a contract other agents can execute for the website account, without a second implementation of work that already exists on draft pull requests.
- State that an account is identity only: email, username, device labels, public keys, and sealed blobs. It is a different database from the heartbeat project. Do not apply identity SQL to the telemetry project.
- State that install and pairing work logged out. The website does not render machine IPs. Password reset does not grant mailbox plaintext. The device private key stays on the machine.
- State that real sign-up waits on Ritik confirming a new $10/month Supabase project. Do not create that project. Do not put keys in git.
- State that a live Jev call waits on a credit card on the Vercel team. Do not call the gateway. Do not commit a key.
- Keep this change to spec files. Do not edit `Shared/Models.swift`, `Shared/MeshClient.swift`, `install/payload/meshd/server.ts`, `project.yml`, or pairing code.

No daemon route, wire type, or pairing payload changes. Install and the 8-character pairing code keep working with no account.

## Capabilities

### New Capabilities

- `account-identity`: What a website account stores, which database it uses, and what install, pairing, the website, password reset, and the device private key do.
- `account-launch-gates`: The two external waits (Ritik's project, a Vercel team card), the ban on keys in git, and the rule that this change does not rebuild finished slices or edit shared contracts.

### Modified Capabilities

- None. `terminal-sessions` stays as it is. The daemon does not learn about accounts.

## Non-goals

- Rebuilding any finished draft named in the design table. That includes the account pages, the identity SQL, the node seal/open helper (pull request 138), the Swift seal check (pull request 141), the menu-bar sealed upload bodies (pull request 152; that file is not in the Xcode target), the key file, the note draft, the Jev filter, the account flow check (pull request 158), the sealed mailbox round trip (pull request 159), the route-gated draft (pull request 160), spoken note in (pull request 162), spoken note to a held file (pull request 164), the account handlers (pull request 165), sealed sync isolation (pull request 166), local sign-up (pull request 167), account delete cascade (pull request 168), the pairing-code hold (pull request 169), account pages on local auth (pull request 170), device and mailbox inserts through local PostgREST (pull request 171), the reset page (pull request 172), account delete against local GoTrue (pull request 175), signed-in device labels (pull request 177), email confirmation before first sign-in (pull request 178), and the confirmation-mail omission proof (pull request 184). Those drafts are the implementation. Re-check each pull request tip before touching the same files, and do not open a second copy.
- Creating a Supabase project, applying identity SQL to the heartbeat project `zmisjteztezaqfflwbgf`, or committing `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, or `AI_GATEWAY_API_KEY`.
- A live call to the Vercel AI Gateway, including `POST https://ai-gateway.vercel.sh/v1/evaluate`.
- A sign-in screen on iPhone, Watch, or the menu-bar app. Native sealed sync stays a later change. It reuses the node seal/open helper on pull request 138, the Swift seal check on pull request 141, the menu-bar sealed upload bodies on pull request 152, and the existing key file. Do not start another copy of any of them.
- A second speech-in path beside pull request 162. That draft does not prove a microphone.
- A price, a Pro flag, a machine cap, a relay, or any edit to `openspec/changes/reach-my-mac-from-anywhere/`.
- Teaching `meshd` about accounts. No new route in `server.ts`.
- Claiming a hosted Supabase project, a production deploy, a live Jev call, or app sign-in. Local proofs stay local.

## Impact

- This change adds only `openspec/changes/account-launch-handoff/`.
- Finished drafts stay where they are: account pages on pull request 150 (`cursor/account-site-e469`, tip `41cdbbc`), identity SQL on pull request 135, the note draft on pull request 154 (tip `cba3ac7`), the key file on pull request 155 (tip `03e40f5`) and pull request 157 (tip `bb1dd5c`, branch `cursor/swift-device-key-e469`), the node seal/open helper on pull request 138 (`cursor/sealed-mailbox-e469`, tip `45904dd`), the Swift seal check on pull request 141 (`cursor/swift-seal-e469`, tip `781613e`), the menu-bar sealed upload bodies on pull request 152 (`cursor/menu-bar-sync-e469`, tip `e890e4d`; that file is not in the Xcode target), the Jev filter on pull request 156, the account flow check on pull request 158 (`cursor/account-flow-stub-e469`, tip `ec5014fd6209f975d0b63614cb50347616551820`), the sealed mailbox round trip on pull request 159 (`cursor/device-sync-roundtrip-e469`, tip `e4dd9e0d90254b14e97548b1419b2bc386cb6c17`), the route-gated draft on pull request 160 (`cursor/note-route-draft-e469`, tip `4c8a73a485cfb7acd6d4c83d577a2cf19f8ab29c`), spoken note in on pull request 162 (`cursor/spoken-note-in-e469`, tip `4e14cab8c9a5edef4f509b7096fcf238f02ffc2d`), spoken note to a held file on pull request 164 (`cursor/spoken-note-to-app-e469`, tip `f8031958bc9778d260931ea189ad002b1a5c5a02`), the account handlers on pull request 165 (`cursor/account-handler-check-e469`, tip `a519e171d909acded25f7b52c7a58f5c08740fc3`; service role stays on the admin delete), sealed sync isolation on pull request 166 (`cursor/sync-rls-proof-e469`, tip `43696bf90ed2fa4d31c6dff6e3c05ccc6cd4b6d2`; SQL sha256 `cd47695328f67abb077b62e48b670ed7cf78d6f4a916348958229d1862445253`), local sign-up on pull request 167 (`cursor/local-auth-proof-e469`, tip `54c896b180453861fc93182fadb9275fc138191f`), account delete cascade on pull request 168 (`cursor/account-delete-cascade-e469`, tip `7ee035703aa08e05102d6a10f013fbc2a6352dab`), the pairing-code hold on pull request 169 (`cursor/jev-pairing-hold-e469`, tip `25c8672a6986df77314d2cc0dae44a8c8ac9d11a`), account pages on local auth on pull request 170 (`cursor/account-pages-local-auth-e469`, tip `e019f19eb7221672799b8cc7827717c425d4f40d`; `account.js` was not edited), device and mailbox inserts through local PostgREST on pull request 171 (`cursor/local-device-rest-e469`, tip `f587e50b47e76f0bcfbafb4f6faa4b30ce8049d5`; user B cannot read user A's rows; daemon and Xcode CI green; identity SQL sha256 `cd47695328f67abb077b62e48b670ed7cf78d6f4a916348958229d1862445253`), the reset page on pull request 172 (`cursor/local-password-reset-e469`, tip `e1ad9c374e704db11a34cc07ce65919238729e66`; the old password does not sign in; daemon and Xcode CI green), account delete against local GoTrue on pull request 175 (`cursor/local-account-delete-e469`, tip `092a3df4db55a2a5092b56f81d74b5bb1ed395e4`; `delete-account.js`; sign-in fails after delete; the other user stays; the handler was not edited; daemon and Xcode CI green), signed-in device labels on pull request 177 (`cursor/account-device-labels-e469`, tip `4cadb598c486af1762c919b885f2a5c2ee26fbeb`; the signed-in page lists label and platform only; empty state is "Pairing still happens on the machine. This page lists labels only."; `account.js` was not edited; daemon CI and apps (Xcode) were green on check run 35709487427), and email confirmation before first sign-in on pull request 178 (`cursor/account-email-confirm-e469`, tip `60fe069f4ecb253048a6f4a9f0455fe56a4c9846`; sign-up returns no session and writes no profile; after the local confirmation link, sign-in writes `{id, username}`; the other user cannot select that profile; `account.js` was not edited; the script turns confirmations on for the run and restores them to off; identity SQL sha256 `cd47695328f67abb077b62e48b670ed7cf78d6f4a916348958229d1862445253`; daemon CI green on check run 35710247197; apps (Xcode) pending when recorded), and the confirmation-mail omission proof on pull request 184 (https://github.com/aryateja2106/lecoder-watch/pull/184, branch `cursor/confirm-mail-leak-e469`, tip `1fbb977cd3c1c43ddfbcbce91edceb55995cc10c`; adds only `experiments/local-auth/prove-confirm-mail.sh`; the local confirmation message, sign-up response, and confirmation redirect omit mailbox ciphertext, the device public key, and a mesh-token sentinel; sign-in writes `{id, username}`; a second user cannot select the first profile, device, or mailbox; the owner reads ciphertext from the database; `account.js` and `001_identity.sql` were not edited; identity SQL sha256 `cd47695328f67abb077b62e48b670ed7cf78d6f4a916348958229d1862445253`; the stack run is on the draft; this handoff did not re-run Docker; daemon CI and apps (Xcode) were both green on check run 35712017549; not the hosted Supabase project). Do not start another copy of any of them.
- The heartbeat database `zmisjteztezaqfflwbgf` is untouched. The only Supabase project in the LeSearch AI org today is that heartbeat project.
- Production `mesh.lesearch.ai` is untouched. The Vercel preview of pull request 150 still redirects to Vercel sign-in.
