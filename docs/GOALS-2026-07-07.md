# MeshWatch Goal — 2026-07-07

Control coding agents on every machine from phone + watch: see all active sessions, enter any machine,
open panes, spawn any CLI agent (claude/codex/pi/anything) with a natural-language task, resume persisted
sessions, get alerted the moment session limits reset, and watch/stop/redirect computer-use agents via VNC.

## Core goals

App-side (Swift) complete for all six after S1–S6 (+ fixup 9c9ad75); remaining gaps are
server-side (S7, owned by Cursor agent) or runtime (runbook), noted per row.

| # | Goal | Status | Shipped / remaining |
|---|------|--------|---------------------|
| G1 | See all sessions; enter machine → new pane → init ANY CLI agent | app done | S3: pi + custom CLI spawn with task, cwd field + recents. Remaining: pane-level one-shot spawn (2-step today), cmux session visibility (S7 bridge) |
| G2 | Natural-language task at spawn | app done | S3: task keeps chosen command (any CLI), not just claude/codex. Remaining: 900ms type-delay race (S7) |
| G3 | Multi-session persistence + resume | app done | S6: watch Kill session restored. Remaining: cmux idle-surface visibility + truthful cmuxSend (S7) |
| G4 | Alert when limits RESET + glanceable usage | app done | S1: reset alert pre-armed from 85% band, survives kill, cold-start tap; S2: Continue unblocks at resetsAt. Remaining: native /limits (OpenUsage-independent, S7 + recipe doc) |
| G5 | VNC mirror of computer-use agents; stop/redirect | app done | S5: one-tap "Watch screen (VNC)" from session peek. Remaining: noVNC :6080 backend not running (runbook) |
| G6 | Usable phone terminal; glance-and-intervene watch | app done | S4: full key rows + spacing-preserving output; S6: watch bare Enter + pinned Continue. Remaining: cmux output paneling/scrollback (S7) |

Commits: S1 `8fc77ac` · S2 `e001161` · S3 `b65a7dc` · S4 `9bbd068` · S5 `67f8d82` · S6 `924d394` · fixup `9c9ad75`.
Verified: both simulator schemes green every slice; all check scripts pass; installed + launched live on iPhone 17 Pro + Watch Series 11 sims (usage glance + service status render real meshd data); independent adversarial review = 0 blockers.

## Slice plan (S1–S6 Swift-only; S7 meshd payload, coordinate with Cursor agent, ship LAST)

- **S1** G4 alert reliability: pre-schedule reset alert from 85% band, cold-start wiring, persist dedupe, surface failures
- **S2** Reset-aware `LimitHelpers.isBlocked` — Continue unblocks the moment resetsAt passes
- **S3** Spawn any CLI with task + cwd (+ recents from pane paths, pi preset, deletion tombstones)
- **S4** Phone peek terminal: full key rows, keep interior blank lines
- **S5** One-tap "Watch screen (VNC)" from session peek
- **S6** Watch: bare Enter, Kill session, pinned Continue from Limits glance, Pi task
- **S7** meshd payload: truthful cmux send, resumable idle surfaces, bridge-down signal in /health, remove hardcoded debug log — **precondition: backport deployed ~/.mesh fixes into install/payload first (sync deployed→repo)**

## Runbook (runtime, not code)

1. cmux.app must be running before the bridge is useful.
2. Bridge :8901 down → `source ~/.mesh/hooks/cmux-bridge.zsh && start_cmux_bridge` from a GUI terminal; if cmux children still hit broken-pipe: `cd ~/.mesh/meshd && nohup bun run cmux-bridge.ts >/tmp/cmux-bridge.log 2>&1 &`
3. Verify chain: `:8901/health` → POST `:8901/cmux tree` → `:8899/agents` shows `cmux:*` rows.
4. Do NOT restart meshd (already runs bridge-aware 0.2.1). After S7 deploys: restart via svc skill only.
5. noVNC :6080 not installer-managed and currently down — needed for G5.
6. arya-pi meshd down (timeout); dataflowagents has nothing on :8899 (needs installer run).
7. NEVER run install.sh before deployed ~/.mesh → repo payload backport (S7 precondition), or deployed fixes regress.
8. Device install: iPhone unlocked; Watch app installs via companion iPhone (RemotePairingError 1035 otherwise).
9. First launch: grant notification permission; set Limit-resume pins for claude + codex in Settings.
10. Keep OpenUsage (:6736) running — meshd /usage mirrors it.

## Ownership split (2026-07-07)

- **Cursor agent (herdr)**: `meshd/`, `install/`, deployed `~/.mesh` — bridge debugging in flight; deployed copies AHEAD of repo payload.
- **Claude (this session)**: Swift app (`iOS/`, `Watch/`, `Shared/`, `scripts/`), S1–S6.
- Branch: `backup/2026-07-02`. Never push main. Slice commits must `git add` only their own files.
