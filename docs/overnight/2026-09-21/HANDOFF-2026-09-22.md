# Handoff — LeSearch AI (lecoder-watch), 2026-09-22 evening

Written from session `c15f5723` after ~24 h of continuous work (overnight run + three review
passes from Arya). The next session picks up from **branch tip `98fda4d`**, everything pushed.

## Where things are

- **Branch:** `feat/lesearch-ai-overnight-2026-09-21`, worktree
  `~/Projects/lecoder-watch/.claude/worktrees/lesearch-ai-overnight` (cut from
  `chore/codebase-map-and-regression`; never merge, never push main). Draft PR:
  https://github.com/aryateja2106/lecoder-watch/pull/133 — body current through the second pass;
  the third pass is in `docs/overnight/2026-09-21/REPORT.md` and needs adding to the PR body.
- **Version:** app **0.8.0** (CFBundleVersion 2) == daemon **0.8.0**. Fleet **mac / pi / jetson all
  on 0.8.0** (`mesh status`, doctor 7/7 each).
- **Tree:** clean (the four `shots/*.png` are rewritten by every full gate; just commit them).
- **Last full gate:** `FACTORY_GATES: level=full status=RED passed=3 failed=1 failing=test`
  at `758a6d1` (gate 6). **Which check is red is NOT known** — the run to name it was interrupted.
  Gate 5 at `473eafa` was GREEN; between them landed: e16610b (launcher/keyboard), f912185
  (stats/handoff/feedback/LA), 3735005 (0.8.0 bump), 758a6d1 (upgrade --src). Likely suspects:
  a check pinning the version string (grep `0.6.0` in `scripts/check-*`), `check-codemap`
  (regenerate with `python3 scripts/codemap-index.py`), `check-ios-smoke` (it rebuilds; watch for
  the sim being busy), or `check-feedback-redact.swift` under check-all's compile flags.
  **First task: `sh scripts/check-all.sh 2>&1 | grep -iE "^FAIL|FAIL:|FAILED"`, fix, rerun
  `./.claude/scripts/gates.sh full`, quote the line verbatim.**

## What the next session should do (in order)

1. Find and fix the red check above; full gate GREEN; commit; push.
2. Update PR #133's body with the third-pass table (copy from REPORT.md "Afternoon, third pass").
3. Arya's hands (say these once, do not repeat):
   - real iPhone: **Apps tab → LeSearch AI → Install** (0.8.0 is served OTA; the phone was
     unreachable by devicectl — cable + `mesh apps install meshwatch` also works); then try the
     remote keyboard, gestures, Live Activity, Choose cards on the phone and the watch.
   - re-pair the real phone + watch to `pi` (its token was rotated twice today).
   - a card on the Vercel AI Gateway team before fx / Jev answer; rotate the gateway key that was
     pasted in chat (it is in `~/.config/secrets.env` as `AI_GATEWAY_API_KEY`).
   - CloudKit container decision for the Hundred app's friend sync.
4. Next slices, per `docs/adr-2026-09-22-platform-shape.md` §6: exposed-secrets reach + pinned
   tasks → VNC credential flow (attended; touches Arya's live Mac) → plugin route loader +
   `docs/plugins/` → artifacts convention + KB search on the phone → installer runs
   hooks/skills install. Then `docs/adr-2026-09-22-on-device-brain.md` (Needle on phone/watch).
5. Open asks from the third review still not built: our own Raycast-class launcher (the
   machine's own launcher and an `/apps` search sheet exist), per-user shortcut sets, a
   real-phone gesture/haptics pass, Hundred friend sync.

## Where the facts live (read these, do not re-derive)

- `docs/overnight/2026-09-21/REPORT.md` — night + three passes, each as a table with proof.
- `docs/overnight/2026-09-21/STATE.md` — slice table (M1–M7 + night); add M8 for the third pass.
- `docs/agents/harnesses.md` — how claude / fx / cursor-agent / agy / pi signal, the 6-step
  onboarding workflow, ranked gaps, prompt-detection spec.
- `docs/adr-2026-09-22-platform-shape.md`, `docs/adr-2026-09-22-on-device-brain.md`,
  `docs/adr-2026-09-22-local-brains-and-streaming-moe.md`, `references/reference-projects.md`.
- `docs/factory/runs/2026-09-22T*` — run records for the morning, the second and third passes.
- Memory (auto-loaded): `overnight-2026-09-21-lesearch-ai`, `usability-morning-2026-09-22`,
  `second-review-2026-09-22` — traps and root causes, including the ones below.

## Traps this session hit (so the next one does not)

- Existing checks pin behaviour: `check-remote-screen-gestures.sh` requires the literal
  `shouldRecognizeSimultaneouslyWith … { true }`; `check-watch-terminal-wiring.sh` requires a
  watch chip for **every** key in the daemon's `KEY_SEND_KEYS`; `check-mesh-chords.sh` requires
  every chord chip's key to be in both hosts' key maps (Linux `KEYSYMS` now lists letters).
  Never edit an existing `scripts/check-*`; add new ones.
- The full gate's sim-fleet check rewrites `docs/overnight/2026-09-21/shots/*.png` — commit
  them after every gate or the stop hook complains.
- `codex exec` in a fresh dir: `-s workspace-write --skip-git-repo-check -C <dir> '<prompt>'`.
- `pkill -f "<string>"` over ssh kills the ssh shell itself when its command line contains the
  string. Never `cat` a systemd unit (it prints `MESHD_TOKEN=`).
- Simulator: the Live Activity lock-screen platter renders empty (WidgetRenderer); the Dynamic
  Island shows the real content on a **signed** sim build (build without
  `CODE_SIGNING_ALLOWED=NO` into `build/DerivedData-signed`). Watch-sim taps via the MCP land
  late/wrong — verify watch UI by build + parser check or on a wrist.
- Sim pairings vanish on every reinstall: `xcrun simctl openurl <udid> "$(mesh pair --json | jq -r .url)"`
  then tap Pair at (354,100); for the Linux boxes mint on the box over ssh
  (`ssh host '~/.mesh/bin/mesh pair --json'`) — daemons only mint on themselves.
- Deploy loop: `sh scripts/package-mesh-install.sh install/dist/mesh-install.tgz && cp
  install/install.sh install/dist/install.sh`, then `mesh upgrade -H pi --src
  https://arya-macbook-pro.tailaddf1e.ts.net:8890/mesh-install.tgz` (works now; same-version
  redeploys need the ssh `install.sh --upgrade --src …` path); Mac: `mesh upgrade --src
  $PWD/install`. The share (`tailscale serve --https=8890` → python http.server :8897 in
  `install/dist`) is still running — turn off when done.
- The Pi keeps a `pi-claude` Claude Code session (auto mode); the Mac's `hundred-days` session
  was killed. Firefox on the Pi is in a "Choose a profile" state from a stray launch — harmless.

## Suggested skills for the next agent

- `oh-my-claudecode:verifier` — fresh-context grading before any "done" claim (writer never grades).
- `codex-delegate` — for Swift/TS slices (fresh briefs, one slice each; the orchestrator builds,
  installs and drives the sim; codex cannot run xcodebuild).
- `agy-delegate` — alternative implementer when Codex quota is low (`mesh usage`).
- `checkpoint` — commit + push after each green slice.
- `notify` — Telegram is unconfigured (no `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID`); use
  SendUserFile for reports instead.
- `native-app-builder` (installed into `~/.agents/skills`) — for the next agent-built app.
- `/factory` — if picking issues from the queue instead of the ADR order.

## Do not

- Merge, push main, edit CHARTER / gates.conf / gates.sh / AGENTS.md / `.github` / existing
  checks or tests unattended (the one exception this session, `UITests/SmokeTests.swift`, was a
  product change Arya directed and is recorded).
- Restart or type into Arya's live Mac from the remote screen; test on the Pi (`arya@arya-pi`,
  X :0) or the Jetson (`aryateja-jetson`).
- Paste auth codes or tokens; never print `~/.mesh/token` or unit files.
