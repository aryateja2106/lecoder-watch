# Agent Estate — full machine inventory (2026-09-01)

Generated by a 6-scout workflow sweeping `$HOME`, `~/Library`, and the external archives.
Raw structured data (646 file entries with byte/token counts): [`agent-estate-raw.json`](agent-estate-raw.json).
Nothing was deleted or moved during this sweep. Token estimates = bytes ÷ 4.

---

## 1. Where the storage goes

### Macintosh HD — "System Data" (127 GB) decoded

System Data = everything Settings can't put in a named bucket. On this machine it reconciles almost exactly:

| Component | Size | What it is |
|---|---:|---|
| `~/Library` (uncategorized part) | ~36 GB | Docker VM **12.1 GB** (`Containers/com.docker.docker`), App Support 18 GB (MacParakeet 3.6, Cursor 3.0, Comet 1.4, FluidAudio 1.1, Superset 1.0, LeVoice 1.0, GhostPepper 1.0), Caches 5.2 GB |
| Hidden dotdirs in `$HOME` | ~32 GB | `.lmstudio` 10.3 (models — keepers), `.local` 6.3, `.codex` 2.2, `.hermes` 2.1, `.cache` 1.8, `.paseo` 1.0, `.cursor` 0.9, `.rustup` 0.8, `.claude` 0.8, `.ollama` 0.8, `.omp` 0.8, `.cargo` 0.7, `go` 0.7, `.yarn` 0.6 … |
| Simulator runtime images | 23.1 GB | `/System/Library/AssetsV2/…SimulatorRuntime` — iOS 26.5 + 27.0, watchOS 26.5 + 27.0 (two full generations) |
| `/Library` | 13.5 GB | Almost all `/Library/Developer` (CoreSimulator caches, toolchains) |
| `/private/var` | 8.8 GB | db 5.0, swap 2.0, folders 1.6 |
| Misc (purgeable, OS-update snapshots, logs, Spotlight) | ~14 GB | Includes 3 `com.apple.os.update` snapshots for tonight's update — leave them |

**Levers, biggest first (none executed — your call):**

1. **Old simulator runtimes ~11 GB** — MeshWatch min target is iOS 26.0 / watchOS 10.0, so 26.5 has min-OS-testing value. If you only ever test on latest:
   ```bash
   xcrun simctl runtime delete 611CE030-D127-4ECD-9A75-4615EFE671E9   # iOS 26.5
   ```
   ```bash
   xcrun simctl runtime delete 2B25BD59-A8A3-41F4-8CAF-C488CEB9A796   # watchOS 26.5
   ```
   (Re-downloadable from Xcode later.)
2. **Docker VM 12.1 GB** — MCP_DOCKER doesn't even connect any more. If Docker Desktop isn't part of the 24/7 plan, prune or uninstall it (you already have colima). `docker system prune -af` first if you keep it.
3. **Abandoned agent dotdirs ~1.8 GB** — see §4.
4. **App caches ~3 GB** — Comet 1.0, Google 0.7, Codex 0.5, orca/omnara updaters 0.7.

### External 2 TB disk

One APFS container, two volumes **sharing free space** (freeing either helps both):

| Volume / folder | Size | Action |
|---|---:|---|
| TM-Backups-MacBook Pro | **1.3 TB** | Your sudo script: `~/2-Areas/machine/delete-old-tm-backups.sh` (keeps 08-31 + 08-29) |
| External Storage → `QUARANTINE-2026-09-01` | **129 GB** | Today's safety net. Set a review date (~2 weeks); delete after confidence |
| External Storage → `June-2026-Reset-Rescue` | 9.6 GB | Review — likely superseded by ARCHIVE |
| External Storage → `ARCHIVE` (28 .tar.zst + manifests) | 8.9 GB | Keep — this is the system |
| External Storage → `ResetPlaybook` | 2.3 GB | Keep or fold into ARCHIVE |

Container free space after both levers: ~432 GB → **~1.85 TB**.

---

## 2. The always-loaded global stack (your fixed token overhead)

Every Claude Code session, before your first word:

| Layer | Tokens |
|---|---:|
| `~/.claude/CLAUDE.md` (OMC orchestration) | ~1,350 |
| `~/.agents/BASE.md` (identity/policy) | ~520 |
| `~/.claude/RTK.md` | ~240 |
| Auto-memory `MEMORY.md` index | ~530 |
| Ponytail SessionStart hook injection | ~800 |
| Skill-listing descriptions (44 global skills + OMC 41 + ponytail 5 + anthropic-skills 25 + built-ins) | **~15–25k** |
| MCP server instructions + deferred-tool name list (Apify, Notion, Railway, playwright, computer-use, claude-in-chrome…) | ~5–10k |

In **lecoder-watch** add: CLAUDE.md ~990 + AGENTS.md ~3,120 + **~80 project skill descriptions** (853 KB of SKILL.md bodies; descriptions alone ≈ 10–12k tokens listed every session).

**A fresh lecoder-watch session costs roughly 35–45k tokens of fixed overhead.** At your volume (57M tokens yesterday) the skill/MCP listings — not your instruction files — are the dominant recurring cost. Your hand-written files are actually lean.

---

## 3. Instruction files — 212 files, ~312k tokens

Full list in `agent-estate-raw.json → instruction_files`. Shape: 102 AGENTS.md, 75 CLAUDE.md, 24 handoff/copilot/conventions, 13 cursor-rules, 4 GEMINI.md.

- **~45% of tokens are not yours** — vendored/reference clones (`vendor/` dirs, `3-Resources/reference`, `go/pkg/mod`). Biggest single file: vendored gbrain CLAUDE.md 161 KB (~40k tok).
- **Top duplication to kill:** `vercel-react-best-practices/AGENTS.md` (20.4k tok) verbatim in both portfolio repos; `multica-arya` vs `multica-arya-latest` (near-dup repos); identical skill-lab CLAUDE.md ×2; hk-explore main vs mcp-ui byte-identical sets; worktree copies (ATR ×4, lecoder-watch ×2); glaze folder duplicated in Downloads (+ zip).
- **The pattern to standardize:** 8 repos already do it right — `CLAUDE.md` containing only `@AGENTS.md`. One source of truth per repo, per-tool files as pointers.
- Deepest stacks: **lecoder-watch** (.claude+.codex+.cursor+.agents+.github + AGENTS/CLAUDE/CONTEXT/MEMORY/HANDOFF), cloudagi/crm, lesearch/lesearch, both portfolio repos.

---

## 4. Agent-tool dotdirs — active vs corpses

**Active:** `.claude` 782M (transcripts 515M, plugins 203M) · `.codex` 2.2G (sessions 1.05G) · `.cursor` 897M (chats 552M) · `.gemini` 235M (incl. antigravity-cli 160M) · `.hermes` 2.1G · `.omp` 770M · `.omnara` 86M · `.lmstudio` 10G (keeper models) · `.ollama` 774M · `.pi` `.agents` `.copilot` `.dsh` `.omc` `.orca` `.cua-driver`.

**Abandoned (>30 days, safe-reclaim ≈ 1.8 GB):**

| Dir | Size | Dead since |
|---|---:|---|
| `.paseo` (models) | 988M | Jun 14 |
| `.jcode` | 226M | Jul 22 |
| `.mempalace` | 208M | Jun 16 |
| `.gbrain` | 151M | Jun 16 |
| `.smolvm` | 109M | Jun 5 |
| `.notebooklm-mcp-cli` | 59M | May 25 |
| `.agentmemory` | 27M | Jun 5 |
| `.claude-free` 11M · `.omx-runs` 13M · `.gjc` `.lescout` `.claude-mem` `.cmux` `.claude-mc` `.cf-agentic` `.mcpc` `.cagent` `.playwright-mcp` `.headroom` | ~30M | May–Jul |

Credential-bearing files noted by name only (not read): `.gemini/oauth_creds.json`, `.omnara/creds.json`, `.pi/agent/auth.json`, `.jcode/*auth*.json`, `.asc/config.json`, `.appstoreconnect/private_keys/AuthKey_*.p8`.

---

## 5. Skills — ~567 SKILL.md machine-wide

- **Canonical pool:** `~/.agents/skills` (35 skills, lockfile v3). `~/.claude/skills` = 24 own + 19 **symlinks** into the pool — the right pattern.
- **The Cloudflare pack (~15 skills) exists as byte-identical physical copies in 5 places** (`.claude`, `.agents`, `.codex`, `.cursor`, `.gemini`). Symlink all of them into `~/.agents/skills` like Claude already does.
- **lecoder-watch carries 89 project skills** — including two nearly-mirrored physical trees (`.claude/skills` + `.agents/skills`, ~80 each). This is your biggest self-inflicted per-session token load, and half the files are duplicated on disk.
- **Name collisions with different content** (know which you're invoking): `tdd`, `ralph`, `remember`, `code-review`, `autopilot`, `prototype`, `handoff` each exist in 2–4 unrelated variants.
- **Heaviest caches:** OMC plugin ~200M (134M marketplace clone, 76M is `.git`), openai-curated-remote 41M, claude-cowork 9.7M.
- The **factory-*** skill set (spec/implement/verify/triage/monitor) lives here in lecoder-watch — the live descendant of Personal-Software-Factory.

---

## 6. Memory — one live system, four corpses

**Live (~73k tokens total):**
- Claude Code auto-memory `~/.claude/projects/*/memory` — 94 files, ~64k tok; **lecoder-watch dominates** (49 files, 164 KB), home-scope 18 files.
- `lecoder-watch/MEMORY.md` (18.5 KB decisions-and-dead-ends ledger).
- `.omc/project-memory.json` in 3 projects (lecoder-watch rescanned today; mostly empty scaffolding).
- SecondBrain-OKF (10.8 MB) — knowledge base, not session memory.

**Dead since June (~387 MB):** `.mempalace` 208M · `.gbrain` 151M · `.agentmemory` 27M · `.claude-mem` 48K. Codex memory tables exist but hold **0 rows**. OMC notepad/wiki surfaces: unused everywhere.

Reality: memory has consolidated onto Claude Code auto-memory + OKF. Everything else is deletable.

---

## 7. Sessions & Wake

Installed **Wake 0.3.2** → `/Applications/Wake.app` (universal, ad-hoc signed; source at `~/tools/Wake`). It indexes read-only and can resume (`claude --resume`, `codex resume`) straight from the UI. ⌘K full-text search across everything.

What it will index here:

| Store | Size | Status |
|---|---:|---|
| `~/.codex/sessions` | 1.1 GB | active (08-31) |
| `~/.cursor/chats` | 579 MB | active today |
| `~/.claude/projects` | 540 MB | active today |
| `~/.gemini/tmp` | 76 MB | dormant since 07-12 |
| `~/.copilot` store | 3.4 MB | dormant 08-12 |
| `~/.pi/agent/sessions` | 20 KB | dormant 08-11 |

cmux's real state is `~/Library/Application Support/cmux` (87 MB) — not a Wake adapter today. The old AgentSessions.app v3.9.3 can be dragged to Trash once Wake proves itself.

---

## 8. The forgotten app: **10x** (+ Glaze, + Personal-Software-Factory)

- **`10x`** — "Cursor for app building": native macOS SwiftUI app; NL prompt → plan → SwiftUI codegen → XcodeGen → `xcodebuild`/`simctl` → simulator preview in-app. Supabase auth, Claude agent loop, projects in `~/Library/Developer/TenXApp/`. Working MVP, last commit 2026-04-19, repo `10x-app-builder/10x`. TestFlight upload was roadmapped ("Later"), never built. The "built around Cursor" memory comes from its ideas.md positioning line.
  - **Retrieve:** `zstd -d "/Volumes/External Storage/ARCHIVE/2026-09-01_work.tar.zst" --stdout | tar -xf - -C ~/restored/ "work/personal/10x"`
  - Its README / ROADMAP / ideas are extracted here: [`10x-README.md`](10x-README.md), [`10x-ROADMAP.md`](10x-ROADMAP.md), [`10x-ideas.md`](10x-ideas.md).
- **Glaze system instructions** — full 8-agent prompt architecture for a Mac-app builder (orchestrator, context gatherer, architects, compliance checker, error investigator; 283 KB concat ≈ 71k tok + 22 skills ≈ 41k tok). Copied to [`../../references/glaze-system-instructions/`](../../references/glaze-system-instructions/).
- **Personal-Software-Factory** — archived at `2026-09-01_Personal-Software-Factory.tar.zst` (597 MB raw): FACTORY.md, MASTER-PLAN.md, ADW pipeline (`aryateja2106/factory`, adw_plan/build/scout/gates), disler forks, and another Glaze copy. The generalized always-on factory; its live descendant is lecoder-watch's factory-* skills.

**LeSearch synthesis:** 10x = the product shape (prompt→iOS app, local-first), Glaze = the prompt architecture (routing + per-domain skills + compliance pass), factory-* skills = the process loop (spec→implement→verify), nl2shell = the local-model command layer. All four ingredients are now referenced from this repo.

---

## 9. Secrets & shell audit

- `~/.zshrc` (486 lines, ~3.5k tok): **no literal secrets**. 21 keys live in `~/.config/secrets.env` (names: AMP, APIFY, ASC×2, BOB, EXA, FAL×2, FIREWORKS, HF, LECODER_SUPABASE, OPENROUTER, POSTHOG, PRINTFUL, STRIPE_ORG, SUPABASE_AGENTFIRST×2, TURNSTILE×2, +2 config vars). Fireworks correctly opt-in via `fw-on`.
- ⚠️ `secrets.env` is sourced into **every** shell — every agent with Bash inherits all 21 keys. Move to the fw-on pattern (or `direnv` per-project) so keys load only where needed.
- Simplification candidates in `.zshrc`: sourcing for tools now dormant (openclaw, daytona completion, Warp block if unused), `nightmode`/`daymode` need sudo anyway, alias sprawl documented only in `aihelp`.

---

## 10. Token-efficiency action plan (ranked by tokens saved × effort)

1. **Split lecoder-watch's 80 project skills into a core set (~15) + on-demand rest** — biggest recurring win (~8–10k tok/session). The design-taste family alone has 8+ overlapping variants.
2. **Trim MCP servers to per-project `.mcp.json`** — global roster (Apify, Notion, Railway, playwright, computer-use, mobbin, cloudflare…) loads instructions everywhere; most matter in one repo each. Matches your "per-project determinism" goal.
3. **Dedupe skills to one pool** — symlink CF pack from `~/.agents/skills` into codex/cursor/gemini; collapse lecoder-watch's mirrored `.claude`+`.agents` trees to one physical tree.
4. **Adopt `CLAUDE.md = @AGENTS.md` everywhere** — one instruction source per repo.
5. **Delete the corpses** — 4 memory systems, 21 stale dotdirs, worktree instruction copies, `multica-arya` (keep `-latest`), Downloads glaze dup + zip.
6. **Prune OMC marketplace clone `.git` (76M)** and legacy `_legacy_shadow_backup` in `~/.claude/skills`.
7. Keep hand-written global files as-is — at ~2.9k tokens combined they're already tight.

---

## 11. Waiting on you

- [ ] `sudo ~/2-Areas/machine/delete-old-tm-backups.sh` → ~1 TB+ back on the external
- [ ] Software update tonight (3 OS-update snapshots are staged for it)
- [ ] Restart cmux for the Shift+Enter keybind
- [ ] Decide: delete iOS/watchOS 26.5 sim runtimes (~11 GB) vs keep for min-OS testing
- [ ] Decide: Docker Desktop stays (prune to ~2 GB) or goes (12 GB back)
- [ ] Set a calendar date to delete `QUARANTINE-2026-09-01` (129 GB)
