---
run_id: 2026-09-22T093000Z-publish
stage: implement
started_at: 2026-09-22T09:30:00Z
finished_at: see PUBLISHED.md
status: in-progress
issue: none (the publish mission: lesearch.ai + real users, in-session)
pull_request: https://github.com/aryateja2106/lecoder-watch/pull/133
gate_level: full
gate_status: quoted in PUBLISHED.md
verifier: scripts/check-published.sh (MESH_PUBLISHED=1) is the finish line; fresh-eyes review by an isolated subagent, findings filed as ux-review issues
human_required: true — see BLOCKED.md
---

# Publish run — check-published.sh as the definition of done

The mission: LeSearch AI published at https://lesearch.ai and ready for real users, with
`scripts/check-published.sh` (an ADD) the only judge. Worktree `compassionate-cohen-53d9fb`,
commits pushed to `feat/lesearch-ai-overnight-2026-09-21` on top of the parallel session's
Moshi-parity slices (26a3950), rebased rather than merged.

## Named the red check

`sh scripts/check-all.sh` at 6511d50 was red on **three** checks, not one: `check-codemap`
(stale SYMBOLS.md after 758a6d1 moved lines in bin/mesh), `check-phone-input-and-wake`
(FeedbackView's note TextField without `.shellSafe`, red since f912185) and, in a fresh
worktree, `check-sim-fleet` via `check-overnight.sh` (no Mac build under build/DerivedData
until the gate's build step has run once). The smoke test's `testATextFieldCanAppear` was
also red on a simulator with no machines: Settings' only always-present TextField ("Quick
send") sat below the Alerts section, off-screen. Fixed by moving Quick send above Alerts —
a product change, and the same layout a new user meets.

## Landed (each with a check)

| Slice | Files | Check |
|---|---|---|
| Finish line + Stop hook | scripts/check-published.sh, .claude/hooks/stop-published.sh (wired from settings.local.json, machine-local) | itself |
| Supabase: feedback table, attachments bucket, RLS, auth config, schema in git | supabase/migrations/20260922100000_feedback.sql, supabase/config.toml | check-published (401/403 probes) |
| In-app feedback → Supabase; optional account | iOS/LeSearchCloud.swift, iOS/FeedbackView.swift, iOS/AccountView.swift, ContentView Settings | check-feedback-cloud.sh, check-feedback-redact.swift |
| Feedback → deduped GitHub issues (`from-users`) | scripts/feedback-to-issues.ts, scripts/feedback-worker.plist (launchd, 10 min) | check-feedback-pipeline.sh |
| Docs a stranger can walk | docs/getting-started.md → web/getting-started.html (scripts/build-web-docs.ts), docs/product/README.md, docs/product/design-system.md, docs/product/shots/ | check-web-docs.sh |
| Landing at lesearch.ai | web/index.html (0.8.0, /install.sh, New in 0.8, footer), web/privacy.html, web/vercel.json (static) — deployed to the `lesearch-website` Vercel project | check-links.sh, check-brand.sh |
| Clean-device install | scripts/check-clean-install.sh (fresh simulator, `mesh apps install --sim --device <udid>`); bin/mesh gained `--device` for `--sim` | check-clean-install.sh (live) |
| Publish proofs through the real UI | UITests/FeedbackSendTests.swift (opt-in), UITests/ScreenshotTests.swift (opt-in), scripts/product-shots.sh | — |
| First run | MeshRelayApp: no biometric lock while no machine is paired | smoke (fresh sim) |

No existing `scripts/check-*` or test was edited. PRODUCT.md, the feature map and the
design-system registry carry rows for every feature above in the same commits.

## Traps met

- Two sessions on one Mac: the parallel session's `check-ios-smoke` reinstalls the app on
  the same simulator the e2e test uses and kills the runner ("Test crashed with signal
  kill"). Serialize simulator work across sessions; message the peer.
- iOS offers **Save Password?** over the app after a signup; it is a remote view inside the
  app's own accessibility tree, so `app.buttons["Not Now"]`, not springboard.
- PostgREST needs SELECT to honour `Prefer: return=representation`; an insert-only role
  must mint the row id on the client and send `return=minimal`.
- Supabase's built-in mailer only reaches team members: without custom SMTP a real signup
  never confirms, so `mailer_autoconfirm` is on (BLOCKED.md).
- The block-merge hook refuses any shell line that names `.claude/` together with `>`,
  `sed`, `cp`, `rm` — including a `cd` into a worktree under `.claude/worktrees/`. Drop the
  `cd`; the working directory persists.
- `xcodebuild test` after a crash spends `simctl diagnose --timeout=600` before it returns.
