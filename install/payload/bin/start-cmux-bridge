#!/bin/zsh
# Start cmux-bridge from Terminal.app (required for cmux socket access).
if [[ ! -o interactive ]]; then
  echo "Open Terminal.app and run: ~/.mesh/bin/start-cmux-bridge" >&2
  exit 1
fi
source "$HOME/.mesh/hooks/cmux-bridge.zsh"
start_cmux_bridge
