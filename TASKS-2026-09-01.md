# Task board — integration + E2E pipeline (started 2026-09-01, resumed 2026-09-02)

Working file, untracked. Owner: the /loop session. States: `[x]` done · `[~]` running · `[ ]` open · `[!]` blocked on Arya.

## A. Notifications rebuild (handoff on `fix/remote-trackpad-back-swipe`, d65f5b1)

- [x] Handoff read. Three leads: nil-category call sites (`iOS/NotificationManager.swift:633`), Live Activity pile-up (`iOS/LiveActivityController.swift:77-82` vs `:235`), redundant surfaces.
- [x] 2026-09-01 workflow run died: every agent hit the account session limit (reset 15:50 IST). Zero results.
- [x] 09:05 relaunch died again at the limit, but the Live Activity finder finished (6 findings). Verified by reading the code: `server.ts:817` adds the session to `laActiveSessions` yet `:821` fires a push-to-start on EVERY alert-worthy event, `:822` sends an update too; the app never observes `Activity.activityUpdates`, so push-started cards never upload an update token and the daemon's `end` finds 0 targets (`push.ts:346`); the keep-one sweep ran only from `MeshRelayApp.init()`.
- [x] **Empirical, from `~/.mesh/agent-events.jsonl` (last 312 events): 0 replyable, 213 `replyable:false`, 99 unset → no notification has ever carried buttons.** mesh-hook detects a session only via `TMUX`/`RMUX` env + `display-message`; every recent event came from a bare terminal / Cursor. 212/312 were `info "Claude event"` whose body was a `.cursor/` transcript PATH (mesh-hook:124 fallback). `la-tokens.json`: 2 start tokens, **zero update tokens**. meshd `/agents` → `[]`.
- [x] Fix written on `fix/notifications-one-per-event` (worktree, off origin/main): push.ts `laRoute` (start for a live session → update to its one token; update with no token reaches nothing, never re-starts), server.ts one LA push per event and no `alert` on it (the alert push is the one banner), LiveActivityController reconciles on every `sync` + observes `activityUpdates`, mesh-hook drops the transcript-path body. New `scripts/check-live-activity-one-card.sh` passes.
- [x] `sh scripts/check-all.sh` on the fix worktree: All self-checks passed — **but `scripts/check-ios-smoke.sh` does not exist** (CLAUDE.md documents it; gone like `gates.sh`), so the suite never compiled the Swift change.
- [x] Committed `d65ab0a`, pushed, **draft PR #120** opened (https://github.com/aryateja2106/lecoder-watch/pull/120).
- [x] `xcodegen generate && xcodebuild` MeshWatch (iOS Simulator, generic) on the worktree → **BUILD SUCCEEDED**, no diagnostics on the changed file. Evidence commented on #120.
- [!] PR #120 stays draft until the pile-up is proven closed on the real phone (needs an APNs round trip: trigger a permission prompt, watch the Lock Screen keep ONE card and clear it).
- [!] **Reply-route decision for Arya:** buttons are only possible when meshd can type into the agent, i.e. a mux-hosted session. Today 0/312 events were mux-hosted, so approve-from-phone has never had a target. Options: run agents under `sess`/herdr (`tmux -L agents`) AND teach mesh-hook + meshd about that socket; or accept IDE/desktop sessions as view-only.
- [ ] Back-swipe fix `8395a94` builds clean but is **not device-verified** — needs a real drag on the phone.

## B. Device screen-testing (the probe)

- [x] `usbmux list` → `[]` (both days). `devicectl` → iPhone connected via Wi-Fi only.
- [x] `uvx pymobiledevice3 developer accessibility list-items` → `ERROR Device is not connected`.
- [!] **Plug the iPhone in via USB.** Re-run the probe; retest capture-while-in-use (4016) and screen recording over USB.
- [ ] Labels → text navigation. No labels → vision via LM Studio per step.

## C. Local inference (cloud branch `claude/local-model-inference-g3i08e`, PR #119)

- [x] Arya's Mference build/test IS in the handoff (1102 tests / 179 suites, 0 failures).
- [x] Fork moved to `~/Projects/Mference`; patch committed `e27bbcb`; pushed `claude/expert-cache-slots`; flag verified in binary.
- [x] `sh scripts/check-all.sh` on the branch, on this Mac: **All self-checks passed, exit 0**.
- [~] `cloud-branch-review` (wf_1db67144, task wu5iwvvrx) relaunched 09:05 after the same limit death.
- [x] Runbook step 3: `qwen36` repacked to `~/models/qwen36.gturbo` (18 GB; 127 → 110 GB free). First launch died on a stale Swift module cache after the move; cleared and rerun.
- [x] **Stale handoff claim:** a local model HAS been running — `~/deepseek-harness/` is a parallel workspace ("LeSearch Mesh × Qwen 3.6 under 2 GB": dsh web + `mference` provider + KV paging ported into `RealForwardRunner`, 16 slots, RSS 1.14 GiB at 32K, measured 2026-09-01). Its `Mference/scratch/qwen36.gturbo` backs the `:8080` server (pid 73312, up since 2026-09-01 noon). The harness spike the handoff calls "never run" is running there. Its AGENTS.md: one MferenceServer at a time.
- [x] Runbook 4+6 at 32 slots (`:8081`, then stopped): **brain-eval 9 pass / 0 fail / 1 unsupported (images)** — function calling, two-turn tool loop, terminal prompt answer, browser sequencing, prefix cache all OK. JSON in scratchpad `qwen36-slots32-2026-09-02.json`. Plain prompt 12.6 s; tool call 21.5 s.
- [x] Graded the user's `:8080` engine identically: **6 pass / 3 fail** — the 3 failures were the first three probes timing out at 120 s (cold after 21 h idle); it then warmed (two-turn loop 89 s, later probes 1-16 s). Not wedged, just cold. JSON in scratchpad `qwen36-dsh-8080-2026-09-02.json`.
- [x] LM Studio server started via `lms server start` (`:1234` up). **It holds no vision model** (only `ornith-1.5-9b-mtp`, `nl2shell-0.8b`, an embedding model) — the product's image path had nothing behind it.
- [x] `lms get` reported "Download completed" for `mlx-community/gemma-4-12B-it-qat-4bit` (11 GB) but nothing landed in `~/.lmstudio/models` (disk fell only 3 GB; job record says completed; same for an Aug-29 GGUF gemma job). LM Studio's own downloader is not trustworthy here.
- [x] `hf download` (with HF_TOKEN from secrets.env; unauthenticated sat at 0 bytes) landed 10 GB at `~/.lmstudio/models/mlx-community/gemma-4-12B-it-qat-4bit` by 2026-09-03 06:40.
- [~] `lms load … --identifier gemma4` + brain-eval `--model gemma4` (image probe should flip to PASS).
- [x] Graded LM Studio as-is (JIT-loaded `ornith-1.5-9b` text model): 7 pass / 1 fail / 2 unsupported — plain prompt returned empty content; image accepted but returned nothing; no `cached_tokens`. JSON: scratchpad `lmstudio-gemma4-2026-09-02.json` (misnamed: no gemma was loaded).
- [x] PR #120 CI: first run RED — `tsc` push.ts(382,21) `Cannot find name 'all'` (my patch inlined the token list the post-send prune still reads). Fixed `bcd7893`, pushed; **CI all green** (daemon + Xcode) as of 2026-09-03 06:41.
- [ ] Runbook 5: RSS/tok-s/TTFT still only partially measured (32-slot RSS 204 MB idle; 16-slot `:8080` 509 MB after 21 h).
- [ ] LM Studio via `svc` (`lms server start`, `lms load gemma-4-12b-it-qat`) and grade it — deferred while two engines and a fleet were competing for RAM.
- [x] **PR #119 verdict: LAND.** Evidence: full `check-all.sh` green on this Mac (the cloud CI could not compile Swift), CI 4/4 green, `MERGEABLE`, and **zero files changed by both main and the branch since their merge-base** (main's 12 install/CLI commits cannot collide). 673 files / 146K lines, 144K of them vendored `references/` (cloud decision, not re-litigated). Caveat for after landing: `loop.ts`/`tools.ts`/`model.ts` (~840 lines) duplicate what dsh already runs in `~/deepseek-harness`; keep `exec.ts`, `meshd.ts`, `mobile.ts`, `risk.ts`, `brain.ts`, `brain-eval/`. The review fleet died twice on the limit; this verdict is from direct verification, not agents.
- [!] Arya: mark #119 ready and merge (agents never merge).
- [ ] Harness spike (deepseek-harness → meshd) BEFORE more mesh-code loop work.

## D. E2E screen-crawler pipeline (the product goal)

- [x] Facts brief written (scratchpad `crawler-facts.md` + `device-handoff.md`). New facts: xcodegen project has NO UI-test target; `idb_companion` installed but `fb-idb` client not; no sims booted; meshd exposes `screenRegion` + `input` (could drive the Mirroring window).
- [~] `screen-crawler-design` workflow, trimmed to 2 designs (native XCUITest, compose-existing) × 2 critics + synthesis; brain-first and release-gate folded into the synthesizer brief. Died on the limit 3×; 6/7 agents cached, synthesis re-running 2026-09-03 06:41. Critic scores: native 4/5 (7 fatal claims), compose 3/6 (8 fatal claims).
- [ ] Land design doc in `docs/` (+ index row); build phase 1 on the simulator; prove it by running.

## E. Repo hygiene

- local `main` = ahead 1 / behind 12; the extra commit `ce2e63f` == `origin/backup/2026-09-01`. Realign (`git reset --hard origin/main`) needs Arya's OK — drops snapshot-only files from the working tree (all recoverable).
- `.claude/scripts/gates.sh` missing though CLAUDE.md documents it. Real gate: `sh scripts/check-all.sh`.
- Root `HANDOFF.md` is a stale July artifact (cloud branch adds a banner).
