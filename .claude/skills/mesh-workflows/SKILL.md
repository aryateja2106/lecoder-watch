---
name: mesh-workflows
description: Router for MeshWatch sessions. Use when starting work, choosing a skill or slash command, or when the user is unsure which workflow to run.
---

# Mesh workflows

Read `docs/agents/workflows.md` and follow the table there. That file is the
index Arya edits. Do not invent a second routing table in this skill.

Then read the chosen skill's `SKILL.md` and run it. If two skills could apply,
use the collision rules in `docs/agents/workflows.md`.
