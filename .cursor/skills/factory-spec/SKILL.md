---
name: factory-spec
description: Turn an ambiguous factory issue into human-approved product, behavior, design, and implementation slices. Use interactively before implementation.
---

# Factory specification for cursor-agent

Read `docs/factory/CONTRACT.md`, `docs/factory/CHARTER.md`, and then the canonical workflow
in `.claude/skills/factory-spec/SKILL.md`. Keep every human approval gate interactive — this
skill is not for unattended runs. Use a fresh session for the critic pass when the workflow
requires one.
